# Normalized Shannon entropy of a terminal cohort's destination distribution

Normalized Shannon entropy of a terminal cohort's destination
distribution

## Usage

``` r
.dest_entropy_norm(destination)
```
