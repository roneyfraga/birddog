% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_formation.R
\name{plot_trajectory_formation}
\alias{plot_trajectory_formation}
\title{Plot the trajectories that fed into a target (confluence timeline)}
\usage{
plot_trajectory_formation(
  formation,
  feeder_curve = c("inflow", "size"),
  max_feeders = 8,
  title = NULL,
  label_size = 4.5,
  year_range = NULL,
  axis_text_size = NULL,
  palette = NULL
)
}
\arguments{
\item{formation}{Output of \code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}}.}

\item{feeder_curve}{What the feeder's growing width traces: \code{"inflow"}
(default) is the cumulative arrival of just the papers that entered the
target, a monotone curve ending at \code{n} on the river's scale; \code{"size"} is the
feeder's measured cluster size per year, ending at \code{cohort_size} (its whole
community, which can dwarf and shrink the river since the 2D scale is
linear). Each feeder rises from zero at its start year to this end value, on
the same proportional scale as the spine, so it grows like the target does.}

\item{max_feeders}{Maximum number of feeders to draw, taken by descending
papers (default: 8). Feeders beyond this are summarised in the caption.}

\item{title}{Plot title. Defaults to the target trajectory key.}

\item{label_size}{Text size for the feeder and target labels (default: 4.5).}

\item{year_range}{Optional \code{c(from, to)} years fixing the time axis, so a short
formation can be shown on the same window as a long one (as in
\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}). \code{NULL} (default) fits the drawn data; a wider
range only extends the axis (it never drops data).}

\item{axis_text_size}{Font size of the x-axis (year) tick labels; \code{NULL}
(default) keeps the built-in size.}

\item{palette}{Optional named colour vector overriding the default hues (keyed
by feeder \code{source_key}), to match colours across plots. \code{NULL} (default) uses
the package's Set2 (<= 8 feeders) / hue scheme.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the target trajectory as a central spine and each feeder as a parallel
timeline that grows along its length (see \code{feeder_curve}) and merges into the
spine at its handoff year. The spine is a cumulative river: it widens smoothly
as each drawn feeder joins, so by the final year its width reflects the total
papers absorbed. Spine and feeder widths share one strictly proportional scale
(width 0 at zero papers), so their thickness is directly comparable. It
generalises \code{\link[=plot_trajectory_dispersion]{plot_trajectory_dispersion()}} (one
source into one absorber) to many feeders converging on one trajectory,
answering visually "which trajectories formed this one?".
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(groups_lineage)
f <- sniff_trajectory_formation(flow, target = "tr::c1g3")
plot_trajectory_formation(f)
}

}
\seealso{
\code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}}, \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}},
\code{\link[=plot_trajectory_dispersion]{plot_trajectory_dispersion()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
