% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_contribution.R
\name{sniff_trajectory_contribution}
\alias{sniff_trajectory_contribution}
\title{Documents an intermediate trajectory contributes to a target in a given year}
\usage{
sniff_trajectory_contribution(flow, source, year, target)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object.}

\item{source}{Trajectory id whose year-node is read, typically an intermediate
\code{trN} (e.g. \code{"tr14"}). Any trajectory id present in \code{flow} is accepted; each
trajectory has at most one node per year.}

\item{year}{Integer publication year. \code{source} must have a node in this year.}

\item{target}{Trajectory id whose final-year community is the contribution
basis, typically a central \code{tr::cNgN}. Its destination \code{group} must be known.}
}
\value{
A tibble with one row per document in \code{source}'s \code{year} node:
\code{source}, \code{target}, \code{year}, \code{document_id}, and \code{in_target} (logical: the
document is in \code{target}'s final-year community). The \code{source}/\code{target}/\code{year}
columns are constant, so results for several (source, year, target) triples
row-bind cleanly. Summarise with one line, e.g.
\code{dplyr::summarise(x, n = dplyr::n(), contributed = sum(in_target))}.
}
\description{
Selects a \code{source} trajectory's node in a specific \code{year} and returns its
documents, flagging which of them belong to the \code{target} trajectory's
final-year community. This is the fine-grained, per-document view of the
inter-trajectory contribution that \code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}} aggregates:
where \code{destination} splits a terminal cohort across every final group,
\code{contribution} answers "which papers of \code{source}, as they stood in \code{year},
consolidate into this one \code{target}".
}
\details{
The result is document ids only (plus the contribution flag), so it composes
with any downstream join: keywords (via the corpus \code{DE} field or
\code{\link[=sniff_groups_keywords]{sniff_groups_keywords()}}), authors, citations, or STM topics. Filter on
\code{in_target} for the contributed set.
}
\seealso{
\code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}}, \code{\link[=sniff_trajectory_self_sufficiency]{sniff_trajectory_self_sufficiency()}},
\code{\link[=sniff_groups_keywords]{sniff_groups_keywords()}}

Other trajectory analysis: 
\code{\link{sniff_trajectory_cct}()},
\code{\link{sniff_trajectory_community}()},
\code{\link{sniff_trajectory_confluence}()},
\code{\link{sniff_trajectory_destination}()},
\code{\link{sniff_trajectory_dynamics}()},
\code{\link{sniff_trajectory_emergence_owners}()},
\code{\link{sniff_trajectory_entropy}()},
\code{\link{sniff_trajectory_formation}()},
\code{\link{sniff_trajectory_hubs}()},
\code{\link{sniff_trajectory_self_sufficiency}()}
}
\concept{trajectory analysis}
