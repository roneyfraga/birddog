% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_confluence.R
\name{sniff_trajectory_confluence}
\alias{sniff_trajectory_confluence}
\title{Build the render-ready confluence forest of the soft DAG}
\usage{
sniff_trajectory_confluence(
  x,
  min_group_size = 10,
  jaccard_min = 0.05,
  k_out = 2
)
}
\arguments{
\item{x}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object, or a \code{docs_per_group} tibble /
\code{\link[=sniff_groups_lineage]{sniff_groups_lineage()}} object / \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object (the
flow is built internally).}

\item{min_group_size, jaccard_min, k_out}{Passed to \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}
when \code{x} is not already a flow object.}
}
\value{
A list with:
\describe{
\item{rivers}{Tibble, one row per trajectory: \code{traj_id}, \code{type}
(\code{"central"}/\code{"absorbed"}), \code{central} (destination group \code{cNgN}), \code{size},
\code{start}, \code{handoff_year} (= \code{last_year} for centrals, else absorption year),
\code{depth}, \code{parent} (absorber \code{traj_id}, \code{NA} for central), and \code{size_curve}
(list-col of \code{tibble(year, size)}, the trajectory's community size per
year).}
\item{confluences}{Tibble, one row per child to parent merge: \code{child},
\code{parent}, \code{n} (papers transferred), \code{cohort_size} (the child's size),
\code{first_feed_year} (publication year of the first transferred paper),
\code{handoff_year}, and \code{inflow_curve} (list-col of \code{tibble(year, size)}).}
\item{destinations}{Tibble, one row per (absorbed trajectory, final group) it
fed: \code{traj_id}, \code{g_final}, \code{n} (papers of the trajectory's terminal cohort
that ended up in \code{g_final}). A trajectory usually appears under several
finals, so this is the multi-destination split the forest collapses to one.}
\item{centrals}{The central group ids (\code{cNgN}) in \code{mixed_sort()} order.}
\item{last_year}{Final year of the analysis.}
}
}
\description{
Turns a \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object into the data a trajectory confluence
needs: one \code{rivers} row per trajectory (with a per-year size curve) and one
\code{confluences} row per child to parent merge edge (papers transferred, the
first year a transferred paper was published, the handoff year, and the
cumulative-inflow curve). Every edge of the flow tree is kept; thresholds are
applied later by \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}, so a slider can re-filter
without recomputing.
}
\seealso{
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}

Other trajectory analysis: 
\code{\link{sniff_trajectory_cct}()},
\code{\link{sniff_trajectory_community}()},
\code{\link{sniff_trajectory_contribution}()},
\code{\link{sniff_trajectory_destination}()},
\code{\link{sniff_trajectory_dynamics}()},
\code{\link{sniff_trajectory_emergence_owners}()},
\code{\link{sniff_trajectory_entropy}()},
\code{\link{sniff_trajectory_formation}()},
\code{\link{sniff_trajectory_hubs}()},
\code{\link{sniff_trajectory_self_sufficiency}()}
}
\concept{trajectory analysis}
