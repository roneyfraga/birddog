% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_groups_influence_network.R
\name{plot_groups_influence_network}
\alias{plot_groups_influence_network}
\title{Plot the group-influence network (the net-influence spine)}
\usage{
plot_groups_influence_network(
  influence,
  weight = c("net", "gross", "surprise"),
  min_weight = 0,
  node_size = c("equal", "io", "balance"),
  colour_role = TRUE,
  edge_labels = TRUE,
  edge_digits = NULL,
  labels = TRUE,
  label_size = NULL,
  layout = "sugiyama",
  title = NULL
)
}
\arguments{
\item{influence}{A \code{\link[=sniff_groups_influence]{sniff_groups_influence()}} object.}

\item{weight}{Which edges and weights to draw: \code{"net"} (default, the net-flow
spine \verb{$net}, one arrow per connected pair), \code{"gross"} (every directed
channel, weighted by citation count), or \code{"surprise"} (every directed
channel, weighted by the size-null surprise).}

\item{min_weight}{Drop edges whose weight is below this (default \code{0}).}

\item{node_size}{How to size the group nodes: \code{"equal"} (default), \code{"io"}
(citations made plus received) or \code{"balance"} (the absolute balance
\eqn{|\beta|}).}

\item{colour_role}{Colour nodes by their source / broker / sink role
(default \code{TRUE}).}

\item{edge_labels}{Print the weight on each edge (default \code{TRUE}).}

\item{edge_digits}{Decimal places for the edge weight labels; \code{NULL}
(default) shows integers for \code{"net"}/\code{"gross"} and two decimals for the
continuous \code{"surprise"} weight.}

\item{labels}{Print the group label on each node (default \code{TRUE}).}

\item{label_size}{Font size of the node labels; \code{NULL} (default) auto-sizes
the nodes and the text so even the smallest node holds its group id.}

\item{layout}{A \code{ggraph}/\code{igraph} layout name (default \code{"sugiyama"}, the
layered DAG layout).}

\item{title}{Plot title; \code{NULL} (default) removes it.}
}
\value{
A \code{ggplot} object.
}
\description{
\emph{Experimental.} Draws the directed influence backbone from a
\code{\link[=sniff_groups_influence]{sniff_groups_influence()}} object as a node-link graph: an arrow runs from the
\strong{source} (the cited, more foundational group) to the \strong{recipient} (the
citing group). By default the edges are the net flow \verb{$net} (\eqn{\nu_{ij} =
C_{ij} - C_{ji}}), the "who, on balance, leads whom" spine; nodes are coloured
by role (source / broker / sink) and sized by citation activity.
}
\examples{
\dontrun{
infl <- sniff_groups_influence(groups)
plot_groups_influence_network(infl)
plot_groups_influence_network(infl, weight = "gross", min_weight = 50)
}

}
\seealso{
\code{\link[=sniff_groups_influence]{sniff_groups_influence()}}, \code{\link[=plot_groups_influence_matrix]{plot_groups_influence_matrix()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
