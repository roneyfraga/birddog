% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft_trajectory_dag.R
\name{.build_global_edges}
\alias{.build_global_edges}
\title{Build the global temporal edge table from docs_per_group}
\usage{
.build_global_edges(
  docs_per_group,
  min_group_size = 10,
  jaccard_min = 0.05,
  k_out = 2
)
}
\arguments{
\item{docs_per_group}{Tibble with \code{group_id} and \code{document_id}.}

\item{min_group_size}{Minimum distinct-document count for a node (default 10).}

\item{jaccard_min}{Minimum Jaccard weight to keep an edge (default 0.05).}

\item{k_out}{Max outgoing edges kept per source node (default 2).}
}
\value{
Tibble with \code{from}, \code{to}, \code{weight}, \code{documents}.
}
\description{
Consecutive-year Jaccard between group-year nodes, reconstructed entirely from
\code{docs_per_group}. Replicates the edge filters of \code{\link[=build_temporal_dag]{build_temporal_dag()}} without
an anchor: keep \code{weight >= jaccard_min}, consecutive years only, top \code{k_out}
outgoing edges per source node.
}
\keyword{internal}
