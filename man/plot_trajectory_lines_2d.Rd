% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_lines_2d.R
\name{plot_trajectory_lines_2d}
\alias{plot_trajectory_lines_2d}
\title{Plot the global trajectories as 2D variable-width lines}
\usage{
plot_trajectory_lines_2d(
  flow,
  target = "all",
  conf = NULL,
  min_n = 5,
  min_prop = 0.05,
  min_total_size = 0,
  min_duration_years = 0,
  min_target_n = 0,
  width_range = c(0.8, 6),
  use_raw_papers = FALSE,
  label_size = 4,
  dest_min_prop = 0.05,
  lowlight_alpha = 0.18,
  lowlight_color = "#9AA5B1",
  legend_ncol = NULL,
  legend_text_size = NULL,
  axis_text_size = NULL,
  year_range = NULL,
  palette = NULL,
  title = NULL
)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object, or a \code{docs_per_group} tibble /
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object the flow is built from.}

\item{target}{What to draw. \code{"all"} (default) draws every central and the
trajectories feeding it; a central \code{traj_id} (e.g. \code{"tr::c1g1"}) or a vector
of them draws only those finals and their feeders.}

\item{conf}{An optional precomputed \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}} object for
\code{flow}; pass it to skip recomputing the confluence on every call (e.g. in a
Shiny app that re-renders as a selector changes). \code{NULL} (default) computes it.}

\item{min_n, min_prop, min_total_size, min_duration_years}{Feeder thresholds,
passed to the same pruning used by \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}: an
intermediate is drawn only if it transfers \verb{>= min_n} papers and \verb{>= min_prop}
of its cohort, has total size \verb{>= min_total_size} and lifespan
\verb{>= min_duration_years}; a pruned intermediate takes its own feeders with it.}

\item{min_target_n}{Minimum papers an intermediate must leave in the selected
final(s) by the last year to be drawn (default \code{0}, no filter). Unlike \code{min_n}
(the tree-edge transfer to its absorber), this counts the intermediate's own
papers that \strong{end up} in the target group, so a feeder that merges in but
barely persists there (e.g. \code{tr39} with 12 of its papers in \code{c1g1}) is dropped
by \code{min_target_n = 20}. Only applies to a targeted final view.}

\item{width_range}{Line-width range \code{c(min, max)} (default \code{c(0.8, 6)}), scaled
by the cumulative tracked documents along each trajectory.}

\item{use_raw_papers}{Scale widths by raw \code{quantity_papers} (\code{TRUE}) or by the
weighted \code{quantity_papers * prop_tracked_intra_group} (\code{FALSE}, default).}

\item{label_size}{Text size of the \code{trN} / \code{tr::cNgN} end labels (default \code{4}).}

\item{dest_min_prop}{In the \code{target = "all"} view only, the minimum share of an
intermediate's terminal cohort that must reach a final for that final to
appear in its legend entry (default \code{0.05}). A targeted view reports only the
selected final(s), so this does not apply.}

\item{lowlight_alpha, lowlight_color}{Opacity and colour of the background
(non-trajectory) edges (defaults \code{0.18}, neutral grey).}

\item{legend_ncol}{Number of columns in the feeding legend; \code{NULL} (default)
lets ggplot choose.}

\item{legend_text_size}{Font size (pt) of the legend text; \code{NULL} (default)
keeps the theme default.}

\item{axis_text_size}{Font size (pt) of the x-axis (year) tick labels; \code{NULL}
(default) keeps the theme default.}

\item{year_range}{Optional \code{c(from, to)} years fixing the time axis, so a short
trajectory can be shown on the same window as a long one (e.g. compare a young
\code{tr::c1g14} against \code{tr::c1g1}). \code{NULL} (default) fits the drawn trajectories;
a wider range only extends the axis (it never drops data).}

\item{palette}{Optional named colour vector overriding the default hues (keyed
by trajectory \code{traj_id}).}

\item{title}{Plot title; \code{NULL} (default) derives one from \code{target}.}
}
\value{
A \code{ggplot} object.
}
\description{
The line counterpart of \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}} over the \strong{global} flow
DAG. Every global trajectory is drawn as a variable-width line through the
year-aware Sugiyama layout: the central finals \code{tr::cNgN} and the intermediate
\code{trN} that feed them, each line widening with the documents accumulated along
its path. A bottom legend reports, for every intermediate, how many papers of
its terminal cohort reached each final group -- a single \code{trN} can feed more
than one final (its terminal cohort splits across groups), which the legend
makes explicit.
}
\details{
\itemize{
\item \strong{x = publication year} (Sugiyama layers); the \strong{y-axis has no intrinsic
meaning} (a branching position).
\item \strong{Line width} grows with cumulative tracked documents along the trajectory.
\item A single-year \strong{feeder} has no edge to draw as a line and is dropped (it
would otherwise show a label/legend with no visible line); a single-year
\strong{central} (the selected final, e.g. a final-year-only community like
\code{tr::c1g17}) is kept and drawn as a point.
\item \strong{Colour} is per trajectory. With a single targeted final the legend is
summarised to \verb{trN: p\% (n)} -- \code{n} of \code{trN}'s papers landed in that final,
which is \verb{p\%} of \code{trN}'s size (the feeders' counts overlap, so they do not
sum to the final's total). With \code{target = "all"} it shows the full split
\verb{trN -> c1g1:n, c1g2:n}. A central is \code{tr::cNgN (final, n)}, where \code{n} is the
documents that \strong{remained} in its final-year community (the last point of
its size curve), not the cumulative documents the trajectory ever held.
}
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
plot_trajectory_lines_2d(flow, min_n = 20)
plot_trajectory_lines_2d(flow, target = "tr::c1g1")
}

}
\seealso{
\code{\link[=plot_trajectory_lines_3d]{plot_trajectory_lines_3d()}}, \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}},
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
