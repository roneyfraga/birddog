% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_group_bipartite.R
\name{plot_trajectory_group_bipartite}
\alias{plot_trajectory_group_bipartite}
\title{Plot the trajectory x group bipartite network}
\usage{
plot_trajectory_group_bipartite(contribution, max_edges = NULL, label_size = 3)
}
\arguments{
\item{contribution}{Output of \code{\link[=sniff_trajectory_group_contribution]{sniff_trajectory_group_contribution()}}.}

\item{max_edges}{Optional integer. If set and the graph has more edges, keep
only the \code{max_edges} heaviest edges (by shared papers) before drawing, so a
dense matrix stays readable. All vertices are kept. Default \code{NULL} (no cap).}

\item{label_size}{Node label text size (default 3).}
}
\value{
A ggplot object.
}
\description{
A two-column bipartite layout: trajectories on the left, final groups on the
right, with diagonal edges whose width is the number of shared papers. A thin
convenience view; for matrix-style (\code{bipartite::visweb}) or nestedness plots,
run the \code{bipartite} package on \code{contribution$incidence} directly.
}
\seealso{
\code{\link[=sniff_trajectory_group_contribution]{sniff_trajectory_group_contribution()}}
}
