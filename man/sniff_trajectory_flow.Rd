% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_flow.R
\name{sniff_trajectory_flow}
\alias{sniff_trajectory_flow}
\title{Detect the flow trajectories (central + absorbed) of the soft DAG}
\usage{
sniff_trajectory_flow(x, min_group_size = 10, jaccard_min = 0.05, k_out = 2)
}
\arguments{
\item{x}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object, or a \code{docs_per_group} tibble /
\code{\link[=sniff_groups_trajectories]{sniff_groups_trajectories()}} object (the DAG is built internally).}

\item{min_group_size, jaccard_min, k_out}{Passed to \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} when
\code{x} is not already a DAG object.}
}
\value{
A list: \code{trajectories} (tibble: \code{traj_id}, \code{type} (\code{"central"}/
\code{"absorbed"}), \code{group} (destination group; NA if extinct), \code{start}, \code{end},
\code{size} (distinct papers), \code{depth} (0 = central), \code{absorbed_into} (absorber
\code{traj_id}, NA for central), \code{absorption_year} (NA for central), \code{nodes}
list-col), \code{tree} (edges \code{child}, \code{parent}, \code{absorption_year}), \code{graph},
\code{docs_per_group}, \code{last_year}.
}
\description{
Decomposes the cumulative-clustering DAG into trajectories via \strong{trunk edges}
(mutual dominance: \code{v -> u} where \code{u} is \code{v}'s heaviest successor and \code{v} is
\code{u}'s heaviest predecessor). Trunk edges form disjoint chains. A chain ending at
a final-year group node is a \strong{central} trajectory (\verb{tr::<group>}, one per final
group, reaches the final year); every other chain is \strong{absorbed} (\verb{tr1 … trN}),
merging into an absorber (central or another absorbed) where its head flows out
via a non-trunk edge. Absorption is transitive, forming a confluence forest.
}
\seealso{
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}, \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}
}
