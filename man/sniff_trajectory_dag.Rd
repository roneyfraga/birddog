% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft_trajectory_dag.R
\name{sniff_trajectory_dag}
\alias{sniff_trajectory_dag}
\title{Build the soft cumulative-clustering trajectory DAG}
\usage{
sniff_trajectory_dag(
  x,
  min_group_size = 10,
  jaccard_min = 0.05,
  k_out = 2,
  similarity = c("overlap", "coupling"),
  references = NULL
)
}
\arguments{
\item{x}{The membership tibble \code{docs_per_group} (\code{group_id}, \code{document_id},
\code{network_until}, \code{group}) or a \code{\link[=sniff_groups_lineage]{sniff_groups_lineage()}} object (its
\verb{$docs_per_group} is used).}

\item{min_group_size}{Minimum distinct-document count for a node (default 10).}

\item{jaccard_min}{Minimum Jaccard weight to keep an edge (default 0.05).}

\item{k_out}{Max outgoing edges kept per source node (default 2).}

\item{similarity}{How to weight consecutive-year edges: \code{"overlap"} (default)
uses the Jaccard of the nodes' shared documents (the cumulative carry-over);
\code{"coupling"} uses the Jaccard of their shared cited references (bibliographic
coupling) and requires \code{references}. Both detectors (\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}},
\code{\link[=sniff_trajectory_channel]{sniff_trajectory_channel()}}) route either DAG unchanged.}

\item{references}{When \code{similarity = "coupling"}, a data frame with columns
\code{document_id} and \code{feature} (one cited reference per row), the same shape as a
\code{content} element of \code{\link[=sniff_trajectory_coherence]{sniff_trajectory_coherence()}}. Ignored for \code{"overlap"}.}
}
\value{
A \code{birddog_dag} object: a classed list with \code{graph} (scored igraph DAG
with vertex attrs \code{year}, \code{size}, \code{group}, \code{is_birth}, \code{terminal_group},
plus \code{doc_ids}), \code{nodes} (tidy tibble \code{name}, \code{year}, \code{group}, \code{size},
\code{is_birth}, \code{terminal_group}), \code{edges} (\code{from}, \code{to}, \code{weight},
\code{documents}), \code{births} (character vector of birth node names), \code{last_year},
and the de-duplicated \code{docs_per_group}.
}
\description{
Reconstructs, from \code{docs_per_group} alone, the full temporal DAG of
cluster-year nodes: vertices are every cluster whose distinct-document count is
at least \code{min_group_size} (isolated nodes included), edges are consecutive-year
Jaccard links (top \code{k_out} per source, \code{weight >= jaccard_min}). Each node is
labelled with the final group its heaviest-successor walk reaches
(\code{terminal_group}) and whether it is a birth (no strong predecessor). This DAG
is the first-class object the soft trajectories and group-formation views are
read from.
}
\seealso{
\code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}

Other trajectory detection: 
\code{\link{is_flow}()},
\code{\link{sniff_trajectory_braid}()},
\code{\link{sniff_trajectory_channel}()},
\code{\link{subset.birddog_flow}()},
\code{\link{validate_flow}()}
}
\concept{trajectory detection}
