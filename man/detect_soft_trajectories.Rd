% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft_trajectory_dag.R
\name{detect_soft_trajectories}
\alias{detect_soft_trajectories}
\title{Detect birth-anchored soft trajectories from the cumulative-clustering DAG}
\usage{
detect_soft_trajectories(
  x,
  min_len = 3,
  min_group_size = 10,
  jaccard_min = 0.05,
  k_out = 2
)
}
\arguments{
\item{x}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object, or a \code{docs_per_group} tibble / a
\code{\link[=sniff_groups_trajectories]{sniff_groups_trajectories()}} object (the DAG is then built internally).}

\item{min_len}{Minimum number of distinct years for a kept trajectory (default 3).}

\item{min_group_size, jaccard_min, k_out}{Passed to \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} when
\code{x} is not already a DAG object.}
}
\value{
A list: \code{graph} (the scored DAG), \code{docs_per_group}, and \code{trajectories},
a tibble sorted by descending \code{size} with columns \code{traj_id}, \code{terminal_group},
\code{birth}, \code{start}, \code{end}, \code{length} (distinct years), \code{size} (distinct papers
across the line's nodes), \code{living} (reaches \code{last_year}), \code{terminal_node},
\code{nodes} (list-col), \code{shares_tail_with} (list-col of other \code{traj_id}s sharing
the terminal node), and \code{merge_year} (year of the earliest node shared with
another trajectory, else \code{NA}).
}
\description{
Walks the principal line of every birth in \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}: from each
birth, follow the single heaviest successor to a final group. Lines that
converge on a common node share every node from there on (the shared tail), so
intermediate nodes can belong to several trajectories — the soft property. Each
trajectory stays a single line \verb{tr1…trN}, ranked by paper count.
}
\seealso{
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}, \code{\link[=sniff_trajectory_group_contribution]{sniff_trajectory_group_contribution()}}
}
