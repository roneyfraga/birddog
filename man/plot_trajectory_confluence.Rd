% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_confluence.R
\name{plot_trajectory_confluence}
\alias{plot_trajectory_confluence}
\title{Plot the trajectory confluence (how the central trajectories form)}
\usage{
plot_trajectory_confluence(
  conf,
  target = "all",
  depth = NULL,
  min_n = 5,
  min_prop = 0.05,
  min_total_size = 0,
  min_duration_years = 0,
  width_range = c(0, 0.42),
  label_terminal = c("id_count", "id"),
  label_intermediary = c("id_count", "id"),
  label_terminal_size = 4.5,
  label_intermediary_size = 4,
  labels_text = NULL,
  legend_ncol = NULL,
  palette = NULL,
  axis_text_size = NULL,
  year_range = NULL,
  show_secondary = TRUE,
  secondary_min_prop = 0.2,
  smooth = TRUE,
  title = NULL
)
}
\arguments{
\item{conf}{A \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}} object.}

\item{target}{What to draw. \code{"all"} (default, same as \code{NULL}) draws the full
confluence (the whole nested forest). \code{"finals"} draws only the \strong{central
backbones} (the inflow spines, tributaries hidden); \code{"intermediary"} draws
only the \strong{tributary streams} (the ribbons, central spines hidden). A single
central \code{traj_id} (e.g. \code{"tr::c1g1"}) draws only that central's tributaries; a
vector of central ids (e.g. \code{c("tr::c1g1", "tr::c1g5")}) draws the selected
centrals stacked. With a single central the streams are coloured per
tributary; otherwise per destination central. A single \strong{intermediate}
trajectory id (e.g. \code{"tr3"}) switches to a \strong{focus view}: its feeders merge
in on the left (who fed it) and the finals its terminal cohort reached fan out
on the right (who it fed).}

\item{depth}{How many confluence levels upstream of the roots to draw; \code{NULL}
(default) means the whole forest when \code{target = NULL}, or just the direct
feeders (\code{1}) when one or more \code{target}s are given. Set e.g. \code{depth = 2} to
expand a target view to feeders-of-feeders.}

\item{min_n}{Minimum papers a tributary must transfer to its absorber to be
counted; below this it is pruned from the drawing \strong{and} the river width,
together with its own upstream subtree (default \code{5}).}

\item{min_prop}{Minimum share of a tributary's own cohort that must transfer
(default \code{0.05}); guards against incidental trickles.}

\item{min_total_size}{Minimum \strong{total size} of a tributary (its own distinct
paper count across all years) for it to be drawn (default \code{0}, no filter);
prunes small lineages and their upstream subtree, complementing \code{min_n}
(which gates the transfer, not the trajectory's own size).}

\item{min_duration_years}{Minimum \strong{lifespan in years} of a tributary (its
last year minus its first year, plus one) for it to be drawn (default \code{0}, no
filter); use it to drop short-lived lineages.}

\item{width_range}{Half-width range \code{c(floor, max)} in y-data units (lanes are
spaced \code{1} apart, so keep \code{max} below ~\code{0.5} to avoid overlap). The widest
stream reaches \code{max}; every band is at least \code{floor}, so thin streams stay
visible. Default \code{c(0, 0.42)} is strictly proportional (no floor); a positive
floor (e.g. \code{c(0.05, 0.45)}) lifts the small streams, raising \code{max} thickens
everything. Note these are lane-fraction units, \strong{not} pixel sizes like
\code{marker_range}.}

\item{label_terminal}{Label content for the \strong{final} (terminal) trajectories:
\code{"id_count"} (default) \code{tr::cNgN (size)}, \code{"id"} \code{tr::cNgN}, or \code{NULL} to hide
them. The count is the central's \strong{final-year} size (what remains in the
final community), matching \code{\link[=plot_trajectory_lines_3d]{plot_trajectory_lines_3d()}}, not its lifetime
total.}

\item{label_intermediary}{Label content for the \strong{intermediate} (tributary)
trajectories: \code{"id_count"} (default) \code{trN (n)}, \code{"id"} \code{trN}, or \code{NULL} to
hide them. \code{n} is the tributary's documents that \strong{end} in its target final
(the destination count from \code{\link[=plot_trajectory_lines_3d]{plot_trajectory_lines_3d()}}) -- the single target
in a single-target view, else the tributary's own dominant central -- not the
trunk transfer. Objects without destination data fall back to \code{trN (n/cohort)}.}

\item{label_terminal_size}{Text size of the \strong{final} (terminal) labels
(default \code{4.5}).}

\item{label_intermediary_size}{Text size of the \strong{intermediate} (tributary)
labels (default \code{4}).}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text} mapping
central group ids (\code{cNgN}) to descriptions; when supplied a bottom legend
\code{tr::cNgN: description} is added (as in \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}). \code{NULL}
(default) adds no legend.}

\item{legend_ncol}{Number of columns in the \code{labels_text} legend; \code{NULL}
(default) lets ggplot choose.}

\item{palette}{Optional named colour vector overriding the default hues (keyed
by destination central in the all/multi view, else by tributary \code{traj_id}).}

\item{axis_text_size}{Font size of the x-axis (year) tick labels; \code{NULL}
(default) keeps the theme default.}

\item{year_range}{Optional \code{c(from, to)} years fixing the time axis (as in
\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}), so a short confluence can share the window of a
longer one. \code{NULL} (default) fits the drawn rivers; a wider range only extends
the axis (it never drops data). Ignored in the single-intermediate focus view.}

\item{show_secondary}{Draw faint links from a tributary to the \strong{other} finals
its terminal cohort fed, beyond the dominant one it merges into (default
\code{TRUE}). A tributary's papers can land in several final groups; the forest
keeps only the dominant, and these links reveal the rest. Only drawn where both
spines and ribbons are shown and the destination central is in view (so the
all-view or several selected centrals, not a single-central view). Needs the
\code{destinations} field of \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}}.}

\item{secondary_min_prop}{Minimum share of a tributary's terminal cohort that
must reach a secondary final for its link to be drawn (default \code{0.2}); raise it
to keep only the strongest secondary destinations.}

\item{smooth}{Gaussian-smooth the stream outlines (default \code{TRUE}): the central
spine's cumulative inflow and each tributary's width follow a ~1-year low-pass
so the edges flow as brush strokes instead of kinked polygons. \code{FALSE} keeps
the raw year-to-year steps.}

\item{title}{Plot title; \code{NULL} (default) derives one from \code{target}.}
}
\value{
A \code{ggplot} object.
}
\description{
A braided river system over \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}}: every trajectory is
a stream flowing left to right along the publication year, merging into its
absorber where it hands off, until the central trajectories (\code{tr::cNgN}) reach
the final year. Each tributary is drawn \strong{grey} until the first paper it
transfers is published, then in its colour; at the merge a band of width equal
to the \strong{transferred} papers (\code{n} of the tributary's \code{cohort_size}) bends into
the river, which widens by that amount. The central spine is centered on its
band with tributaries packed above and below. With \code{target = NULL} the whole
system is drawn (one colour per destination central); with \code{target = "tr::c1g1"}
only that central's direct feeders are drawn (one colour per tributary).
}
\details{
\itemize{
\item \strong{x = publication year} (never reassigned); the \strong{y-axis has no intrinsic
meaning}, it only packs streams to minimise merge crossings.
\item \strong{Tributary width} follows its community size each year; \strong{grey} before the
first transferred paper, then its \strong{colour}. \strong{Central spine width} is the
smoothed cumulative inflow of its counted tributaries (papers transferred),
on one proportional scale shared with the tributaries (0 papers give 0 width).
\item A tributary hands \code{n} of its \code{cohort_size} papers over; only \code{n} enters the
river, so the merge band keeps a \strong{constant width equal to the transferred
papers} (the \code{n/cohort} label makes the split explicit) and bends over a span
that scales with the distance travelled, so its thickness reads the same for
near and far feeders.
\item \code{min_n} / \code{min_prop} decide what is \strong{counted}, not merely hidden: dropped
tributaries leave the river narrower and are summarised in the caption.
\item An intermediate that delivers \strong{0 documents} to the final it flows into (its
terminal cohort lands entirely outside that final's community) is always
excluded, together with its upstream subtree, regardless of the thresholds: a
stream contributing nothing to the destination cannot appear.
}
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
conf <- sniff_trajectory_confluence(flow)

# the whole system: how every central forms
plot_trajectory_confluence(conf, min_n = 20)

# one central's direct feeders, coloured per tributary
plot_trajectory_confluence(conf, target = "tr::c1g1")

# focus on one intermediate trajectory: its feeders + the finals it fed
plot_trajectory_confluence(conf, target = "tr7")

# two selected centrals, a description legend, bigger year labels, bare ids,
#  a visible floor on thin streams, and only sizeable tributaries
descr <- data.frame(id = c("c1g1", "c1g5"), text = c("Topic A", "Topic B"))
plot_trajectory_confluence(conf, target = c("tr::c1g1", "tr::c1g5"),
                              labels_text = descr, axis_text_size = 14,
                              label_intermediary = "id", min_total_size = 30,
                              min_duration_years = 3, width_range = c(0.05, 0.45))
}

}
\seealso{
\code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}}, \code{\link[=plot_trajectory_formation]{plot_trajectory_formation()}},
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
