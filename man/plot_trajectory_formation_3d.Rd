% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_formation_3d.R
\name{plot_trajectory_formation_3d}
\alias{plot_trajectory_formation_3d}
\title{Plot the trajectories that fed into a target in 3D (confluence)}
\usage{
plot_trajectory_formation_3d(
  formation,
  feeder_curve = c("size", "inflow"),
  max_feeders = 8,
  log_scale = TRUE,
  width_range = c(3, 12),
  title = NULL,
  label_size = 16,
  hover_font_size = 12
)
}
\arguments{
\item{formation}{Output of \code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}} (or one element of
\code{\link[=sniff_trajectory_formations]{sniff_trajectory_formations()}}).}

\item{feeder_curve}{What the feeder slope traces: \code{"size"} (default) is the
feeder's measured cluster size per year, ending at \code{cohort_size} (can rise
and fall as the community merges and splits; best read with
\code{log_scale = TRUE}); \code{"inflow"} is the cumulative arrival of just the \code{n}
contributed papers, a monotone curve ending at \code{n} on the river's scale.}

\item{max_feeders}{Maximum number of feeders to draw, by descending papers
(default: 8). Papers in omitted feeders are noted in the title.}

\item{log_scale}{Apply \code{log1p()} to the z-axis (papers), which keeps large
feeders from dwarfing the river (default: TRUE).}

\item{width_range}{Line-width range, scaled by papers (default: c(3, 12)).}

\item{title}{Plot title (default: the target trajectory key).}

\item{label_size}{Font size for the end labels (default: 16).}

\item{hover_font_size}{Font size for hover tooltips (default: 12).}
}
\value{
A plotly 3D plot object.
}
\description{
Interactive 3D (plotly) version of \code{\link[=plot_trajectory_formation_2d]{plot_trajectory_formation_2d()}}, in the
idiom of \code{\link[=plot_group_trajectories_lines_3d]{plot_group_trajectories_lines_3d()}}. The target trajectory is a
cumulative "river" line at lane \code{y = 0} whose height (z) grows with the papers
absorbed. Each feeder sits on its own lane and rises along a measured curve
(see \code{feeder_curve}); a connector then drops from the feeder's end onto the
river at its handoff year, its width scaled by the papers \code{n} that actually
transferred. So the slope shows how each feeder built up and the connector
shows how much of it flowed into the target.
}
\examples{
\dontrun{
f <- sniff_trajectory_formation(
  "c1g10::tr1", groups_detected_trajectories,
  groups_cumulative_trajectories$docs_per_group
)
plot_trajectory_formation_3d(f)
}

}
\seealso{
\code{\link[=plot_trajectory_formation_2d]{plot_trajectory_formation_2d()}}, \code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}},
\code{\link[=plot_group_trajectories_lines_3d]{plot_group_trajectories_lines_3d()}}
}
