% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_handoff.R
\name{plot_trajectory_handoff}
\alias{plot_trajectory_handoff}
\title{Plot the handoff from a stagnant trajectory to the one that absorbed it}
\usage{
plot_trajectory_handoff(destination, title = NULL, label_size = 5.5)
}
\arguments{
\item{destination}{Output of \code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}} built with
\code{all_detected} (so that \code{source_info} and \code{continuation_info} are present).}

\item{title}{Plot title. Defaults to a \code{source -> absorber} summary.}

\item{label_size}{Text size for the in-plot labels (the two trajectory names
and the papers-carried annotation). Default: 5.5.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the stagnant (source) trajectory and the dominant absorbing trajectory
as two timelines on a shared year axis, with an arrow marking the handoff
year (the source's terminal year). It answers, visually, "which living
trajectory continued this one?".
}
\examples{
\dontrun{
dest <- sniff_trajectory_destination(
  detected, "tr3", docs_per_group,
  all_detected = groups_detected_trajectories, group = "c1g10"
)
plot_trajectory_handoff(dest)
}

}
\seealso{
\code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}}, \code{\link[=plot_trajectory_destination]{plot_trajectory_destination()}}
}
