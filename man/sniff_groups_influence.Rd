% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_groups_influence.R
\name{sniff_groups_influence}
\alias{sniff_groups_influence}
\alias{is_influence}
\alias{print.birddog_influence}
\title{Measure directed citation influence between research groups}
\usage{
sniff_groups_influence(groups, self = TRUE, null_reps = 0, seed = NULL)

is_influence(x)

\method{print}{birddog_influence}(x, ...)
}
\arguments{
\item{groups}{A \code{\link[=sniff_groups]{sniff_groups()}} object: a list with a \code{network} component, a
directed \code{tidygraph}/\code{igraph} whose nodes carry a \code{group} attribute and
whose edges are the internal citations (citing -> cited).}

\item{self}{Keep the diagonal (intra-group citations) in the matrix and in
the out/in strengths. Default \code{TRUE}. \code{FALSE} studies only between-group
flow.}

\item{null_reps}{Number of group-label permutations for a per-channel
p-value: shuffle the group labels over documents, recompute the
cross-citation matrix, and record how often the random flow meets or beats
the observed count. Default \code{0} (no p-values). When \verb{> 0}, \code{flow} gains a
\code{p_value} column.}

\item{seed}{Optional integer seed for the permutation null, for
reproducibility. Default \code{NULL}.}

\item{x}{A \code{birddog_influence} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{birddog_influence}, a list with:
\itemize{
\item \code{matrix}: the \eqn{G \times G} cross-citation matrix \eqn{C} (rows
cite columns), groups in \code{mixed_sort()} order.
\item \code{flow}: a tibble, one row per observed ordered pair, sorted by
descending \code{surprise} -- \code{influencer} (cited group), \code{recipient}
(citing group), \code{citations}, \code{debt}, \code{audience}, \code{salton}, \code{surprise}
(and \code{p_value} when \code{null_reps > 0}).
\item \code{groups}: per-group \code{received}, \code{made}, \code{balance}, \code{role}
(source / broker / sink), sorted by descending \code{balance}.
\item \code{net}: the net-influence edge list -- \code{from} (source), \code{to}
(recipient), \code{net} (\eqn{\nu_{ij} > 0}).
\item \code{params}: the call settings.
}
}
\description{
\emph{Experimental.} Lifts the internal citations of a \code{\link[=sniff_groups]{sniff_groups()}} network to
the group level and measures how much each group's output flows into every
other group's. Influence is \strong{directional}: if group \eqn{B}'s papers cite
group \eqn{A}'s papers, knowledge flows \eqn{A \to B}. The function returns
the cross-citation matrix, four size-corrected indices per ordered pair, the
net flow between pairs, and a source / broker / sink role per group.
}
\details{
Writing \eqn{C_{ij}} for the number of citations from group \eqn{i} to group
\eqn{j} (rows cite columns), out-strength \eqn{o_i=\sum_k C_{ik}}, in-strength
\eqn{\iota_j=\sum_k C_{kj}}, and total \eqn{m=\sum_{ij}C_{ij}}, each ordered
pair carries four normalizations:
\itemize{
\item \strong{debt} \eqn{C_{ij}/o_i} -- the share of \eqn{i}'s citations owed to
\eqn{j} (how much \eqn{i} leans on \eqn{j});
\item \strong{audience} \eqn{C_{ij}/\iota_j} -- the share of \eqn{j}'s citations
coming from \eqn{i};
\item \strong{salton} \eqn{C_{ij}/\sqrt{o_i\,\iota_j}} -- a size-free channel
strength, symmetric in the pair;
\item \strong{surprise} \eqn{C_{ij}/(o_i\,\iota_j/m)} -- the flow against the
configuration-model expectation; \eqn{>1} over-represented, \eqn{<1}
under-represented.
}
The \strong{net influence} \eqn{\nu_{ij}=C_{ij}-C_{ji}} removes reciprocal flow,
and the group \strong{balance} \eqn{\beta_i=\iota_i-o_i} (received minus made)
classifies each group as a \emph{source} (\eqn{\beta>0}, foundational), a \emph{sink}
(\eqn{\beta<0}, frontier consumer), or a \emph{broker} (\eqn{\beta\approx 0}). The
balances of a closed system sum to zero. The diagonal \eqn{C_{ii}} is
intra-group cohesion; \code{self = FALSE} drops it before any normalization (the
balances and net are unchanged, since the diagonal cancels).
}
\examples{
\dontrun{
groups <- sniff_groups(net)
infl <- sniff_groups_influence(groups)
infl
infl$groups          # source / broker / sink per group
infl$net             # who, on balance, leads whom
plot_groups_influence_matrix(infl)
}

}
\seealso{
\code{\link[=sniff_groups]{sniff_groups()}}, \code{\link[=sniff_groups_hubs]{sniff_groups_hubs()}},
\code{\link[=plot_groups_influence_matrix]{plot_groups_influence_matrix()}}, \code{\link[=plot_groups_influence_network]{plot_groups_influence_network()}}

Other groups (stock): 
\code{\link{sniff_components}()},
\code{\link{sniff_groups}()},
\code{\link{sniff_groups_attributes}()},
\code{\link{sniff_groups_cumulative}()},
\code{\link{sniff_groups_cumulative_citations}()},
\code{\link{sniff_groups_hubs}()},
\code{\link{sniff_groups_lineage}()},
\code{\link{sniff_network}()}
}
\concept{groups (stock)}
