% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_dynamics.R
\name{data_state_thresholds}
\alias{data_state_thresholds}
\title{Data-driven, outlier-robust dynamic-state thresholds}
\usage{
data_state_thresholds(dyn, k = 1)
}
\arguments{
\item{dyn}{A \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} tibble; only its metric columns are
read, so the classification it currently carries is irrelevant.}

\item{k}{Number of robust deviations (scaled MAD) above/below the median for the
growth cuts (default 1): \code{emergence_growth = median + k*MAD} and
\code{decline_growth = median - k*MAD} over the living cores' \code{growth_rate}. Larger
\code{k} widens the maturity band (fewer emergence / dormancy calls).}
}
\value{
A list shaped like \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}:
\code{emergence_growth}, \code{decline_growth} (robust, from the centrals' growth),
\code{emergence_novelty} (median novelty of the centrals), \code{convergence_entropy}
(median destination entropy of the absorbed), and \code{dormancy_share} (the
default majority rule). Any quantity that cannot be derived (too few points,
zero spread) falls back to its \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}} value.
}
\description{
Derives a \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}-shaped list from an observed
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} table, so the cut points adapt to the dataset
instead of fixed constants. The growth cuts are placed \code{k} robust deviations
(median absolute deviation) from the median, which resists outliers such as a
single fast-growing young core; the bounded metrics use their median as the
neutral split, and \code{dormancy_share} keeps the absolute majority rule (its
distribution is usually too concentrated at zero to standardize).
}
\details{
Use it as a drop-in for the \code{thresholds} argument; the metrics are
threshold-independent, so one pass is enough to derive and re-classify:
\code{dyn <- sniff_trajectory_dynamics(flow)},
\code{th <- data_state_thresholds(dyn)},
\code{dyn <- sniff_trajectory_dynamics(flow, thresholds = th)}.
}
\seealso{
\code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}, \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}},
\code{\link[=plot_trajectory_dynamics]{plot_trajectory_dynamics()}}
}
