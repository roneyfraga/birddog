% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_dynamics.R
\name{fixed_state_thresholds}
\alias{fixed_state_thresholds}
\title{Fixed-constant thresholds for dynamic-state classification}
\usage{
fixed_state_thresholds()
}
\value{
A list:
\code{emergence_growth} (min annualized \code{growth_rate} for a central to count as
emergence; default 0.15), \code{emergence_novelty} (min \code{novelty} for emergence;
default 0.30), \code{decline_growth} (a central at or below this \code{growth_rate} is
dormant by stall; default -0.05), \code{convergence_entropy} (an absorbed
trajectory whose normalized destination entropy is below this converged, else
diverged; default 0.5), \code{dormancy_share} (an absorbed trajectory whose
terminal cohort drops at least this share is dormant by death; default 0.5).
}
\description{
The fixed knobs for \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}. These are \strong{not} the applied
default: by default the cut points are derived from the data via
\code{\link[=data_state_thresholds]{data_state_thresholds()}}, and these constants serve as the explicit
fixed-constant option (pass this list as \code{thresholds}) and as the fallback for
any quantity that cannot be derived. The classification follows two axes (see
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}): a life-cycle \strong{phase} for living (central)
trajectories and a terminal \strong{fate} for declining (absorbed) ones.
}
