% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_destination.R
\name{.destination_paths}
\alias{.destination_paths}
\title{Modal year-group path of each destination}
\usage{
.destination_paths(
  flow,
  key_col = "final_group",
  share_tbl = NULL,
  id_col = "g_final",
  min_prop = 0
)
}
\arguments{
\item{flow}{Flow tibble with \code{from_id}, \code{to_id}, \code{from_year}, \code{to_year}, the
colour key column, and \code{n}.}

\item{key_col}{Colour key column in \code{flow} (\code{"final_group"} or \code{"dest_traj"}).}

\item{share_tbl}{Destination share tibble with an id column and \code{prop}.}

\item{id_col}{Id column in \code{share_tbl} (\code{"g_final"} or \code{"traj_key"}).}

\item{min_prop}{Minimum cohort share to keep a destination in its own band.}
}
\value{
Tibble \code{dest}, \code{year}, \code{node}, \code{papers}.
}
\description{
From a \code{flow} tibble (see \code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}}) derives, for each
destination and year, the group that holds most of that destination's papers
and the number of papers present that year. Sub-threshold destinations are
merged into \code{"(other)"}.
}
\keyword{internal}
