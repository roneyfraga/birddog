% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_cct.R
\name{sniff_trajectory_cct}
\alias{sniff_trajectory_cct}
\title{Per-year citation cycle time (renewal pace) along each flow trajectory}
\usage{
sniff_trajectory_cct(flow, references)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object.}

\item{references}{A long data frame with columns \code{document_id} and \code{ref_age},
one row per document-cited-reference, \code{ref_age} the reference's age in years
(\code{citing_year - cited_year}). Build it from the corpus \code{CR} field joined to the
cited works' publication years (e.g. the \code{tracked_cr_py} of
\code{\link[=sniff_cct]{sniff_cct()}}).}
}
\value{
A tibble, one row per trajectory: \code{traj_id}, \code{type}, \code{group}, and \code{cct},
a list-column whose each cell is a tibble with columns \code{year} and \code{cct} (the
median citation age over that node-year's documents; \code{NA} when none of them
carry references).
}
\description{
Recomputes the Citation Cycle Time (Kayal, 1999) from the documents in each
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} trajectory's own group-year nodes, one value per
year of the trajectory's life: per node-year, the median over its documents of
each document's median cited-reference age. A low or falling series marks a
fast-renewing knowledge stock, a diagnostic signal of emergence.
}
\details{
This is the faithful, per-document counterpart of \code{\link[=sniff_cct]{sniff_cct()}}
(which scores a static \code{sniff_groups()} partition by group-year): it follows the
trajectory's actual membership, so an absorbed tributary is scored on its own
documents rather than its destination group's.
}
\references{
Kayal AA, Waters RC (1999). An empirical evaluation of the technology
cycle time indicator. IEEE Trans. Eng. Manag. 46(2):127-31.
}
\seealso{
\code{\link[=sniff_cct]{sniff_cct()}}, \code{\link[=sniff_trajectory_entropy]{sniff_trajectory_entropy()}},
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}, \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}

Other trajectory analysis: 
\code{\link{sniff_trajectory_community}()},
\code{\link{sniff_trajectory_confluence}()},
\code{\link{sniff_trajectory_contribution}()},
\code{\link{sniff_trajectory_destination}()},
\code{\link{sniff_trajectory_dynamics}()},
\code{\link{sniff_trajectory_emergence_owners}()},
\code{\link{sniff_trajectory_entropy}()},
\code{\link{sniff_trajectory_formation}()},
\code{\link{sniff_trajectory_hubs}()},
\code{\link{sniff_trajectory_self_sufficiency}()}
}
\concept{trajectory analysis}
