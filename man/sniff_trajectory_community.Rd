% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_community.R
\name{sniff_trajectory_community}
\alias{sniff_trajectory_community}
\title{Community breadth (distinct authors) of each flow trajectory}
\usage{
sniff_trajectory_community(flow, authors)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object.}

\item{authors}{A long data frame with columns \code{document_id} and \code{author}, one
row per document-author pair (e.g. the corpus \code{AU} field split on \verb{;} and
joined to the membership key). Documents absent from \code{authors} contribute no
authors but still count in \code{n_docs}, so keep author coverage high.}
}
\value{
A tibble, one row per trajectory: \code{traj_id}, \code{type}, \code{group},
\code{n_docs} (distinct documents of the trajectory), \code{n_authors} (distinct
authors across them), \code{authors_per_doc} (\code{n_authors / n_docs}, the breadth;
\code{NA} for an empty trajectory).
}
\description{
Measures the FUSE \emph{community} pillar (Carley et al., 2018) for every
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} trajectory: how many distinct authors stand behind
its documents, and how broad that authorship is per document. A raw author
count tracks trajectory size; \code{authors_per_doc} is the size-independent breadth
--- a high value marks a lineage carried by many distinct contributors (a wide
community), a low value a lineage concentrated in a tight group.
}
\details{
This is the document-cluster analogue of the term-level community measure in
the emergence-indicator literature, where a term counts as emergent only when
used by more than one independent author. It complements
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}, which names the community pillar but scores it
only through document counts (\code{recruitment}, \code{size}).
}
\seealso{
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}, \code{\link[=sniff_trajectory_hubs]{sniff_trajectory_hubs()}},
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}

Other trajectory analysis: 
\code{\link{sniff_trajectory_cct}()},
\code{\link{sniff_trajectory_confluence}()},
\code{\link{sniff_trajectory_contribution}()},
\code{\link{sniff_trajectory_destination}()},
\code{\link{sniff_trajectory_dynamics}()},
\code{\link{sniff_trajectory_emergence_owners}()},
\code{\link{sniff_trajectory_entropy}()},
\code{\link{sniff_trajectory_formation}()},
\code{\link{sniff_trajectory_hubs}()},
\code{\link{sniff_trajectory_self_sufficiency}()}
}
\concept{trajectory analysis}
