% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_dynamics.R
\name{plot_trajectory_dynamics}
\alias{plot_trajectory_dynamics}
\title{Strategic map of trajectory dynamic states}
\usage{
plot_trajectory_dynamics(
  dyn,
  target = c("finals", "intermediary"),
  thresholds = NULL,
  labels_text = NULL,
  label_id = FALSE,
  size_range = c(2, 14),
  label_size = 3.5,
  axis_text_size = NULL,
  palette = NULL,
  show_thresholds = TRUE,
  show_quadrants = TRUE,
  xlim = NULL,
  ylim = NULL,
  legend_position = "right",
  title = NULL
)
}
\arguments{
\item{dyn}{A \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} tibble.}

\item{target}{What to draw, following \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}. \code{"finals"}
(default) maps the \strong{living (central)} trajectories on a life-cycle plane
(novelty x growth) that separates emergence, maturity and dormancy.
\code{"intermediary"} maps the \strong{declining (absorbed)} trajectories on a
destination plane (entropy x dormant share) that separates convergence,
divergence and dormancy. The two populations live on different axes, so there
is no combined view.}

\item{thresholds}{The \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}-shaped list whose cut values
bound the shaded regions and the reference lines. \code{NULL} (default) reuses the
cuts the classification was built with -- \code{attr(dyn, "state_thresholds")},
attached by \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} -- so regions and colours always
agree; if that attribute is absent it derives them from \code{dyn} via
\code{\link[=data_state_thresholds]{data_state_thresholds()}}. Pass an explicit list to override.}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text} mapping
group ids (\code{cNgN}) to descriptions; used to label the living cores in the
\code{"finals"} view (as in \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}). \code{NULL} (default) labels by id.}

\item{label_id}{Prefix each living-core label with its group id, as
\code{cNgN: description} (default \code{FALSE}, description only). No effect when
\code{labels_text} is \code{NULL} (the label is already the id) or in the
\code{"intermediary"} view (labelled by \code{traj_id}).}

\item{size_range}{Point-area range \code{c(min, max)} passed to \code{\link[ggplot2:scale_size]{ggplot2::scale_size()}}
(default \code{c(2, 14)}).}

\item{label_size}{Text size of the point labels (default \code{3.5}); the region
labels are drawn one point larger.}

\item{axis_text_size}{Font size of the axis tick labels; \code{NULL} (default) keeps
the theme default.}

\item{palette}{Optional named colour vector overriding the default semantic
hues, keyed by state (\code{emergence}/\code{maturity}/\code{dormancy} for \code{"finals"},
\code{convergence}/\code{divergence}/\code{dormancy} for \code{"intermediary"}).}

\item{show_thresholds}{Draw the classification cut lines (default \code{TRUE}).}

\item{show_quadrants}{Shade and name the state regions (default \code{TRUE}).}

\item{xlim, ylim}{Optional \code{c(lo, hi)} to zoom the axes via
\code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} without dropping data from the fit; \code{NULL}
(default) expands the view to keep every state region visible (the data, the
threshold cuts, and the \verb{[0,1]} share bounds, with padding), so the bands are
shown even when the points cluster. Pass a range to override, e.g. to zoom
past a single young core's outlying \code{growth_rate}.}

\item{legend_position}{Legend placement (default \code{"right"}).}

\item{title}{Plot title; \code{NULL} (default) derives one from \code{target}.}
}
\value{
A \code{ggplot} object.
}
\description{
A quadrant scatter of \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} output, one figure per
population, designed to make the dynamic states legible at a glance. Each point
is a trajectory, its area the community size, its colour the state; the regions
are shaded and named after the state they classify, with the thresholds drawn as
reference lines.
}
\details{
\itemize{
\item \strong{\code{"finals"}} -- x = \code{novelty}, y = \code{growth_rate}; emergence is the
novel-and-growing region, dormancy the loss-of-momentum band, maturity the
saturated middle.
\item \strong{\code{"intermediary"}} -- x = \code{dest_entropy}, y = \code{dormant_share}; convergence is
the low-entropy (single destination) side, divergence the high-entropy side,
and dormancy the high-dropout band (rare, so this band is usually sparse).
Extinct (group \code{NA}) lineages have no coordinates and are reported in the
caption rather than drawn.
}
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
dyn  <- sniff_trajectory_dynamics(flow)
descr <- data.frame(id = "c1g1", text = "Anaerobic digestion")

plot_trajectory_dynamics(dyn, target = "finals", labels_text = descr)
plot_trajectory_dynamics(dyn, target = "intermediary")
}

}
\seealso{
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}, \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}},
\code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
