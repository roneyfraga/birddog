% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft_trajectory_dag.R
\name{as_trajectory_dag}
\alias{as_trajectory_dag}
\title{Coerce trajectory-layer input to a soft DAG}
\usage{
as_trajectory_dag(x, min_group_size = 10, jaccard_min = 0.05, k_out = 2)
}
\arguments{
\item{x}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object, a \code{\link[=sniff_groups_lineage]{sniff_groups_lineage()}} list,
or a \code{docs_per_group} tibble.}

\item{min_group_size}{Minimum distinct-document count for a node (default 10).}

\item{jaccard_min}{Minimum Jaccard weight to keep an edge (default 0.05).}

\item{k_out}{Max outgoing edges kept per source node (default 2).}
}
\value{
A \code{birddog_dag} object.
}
\description{
Resolves the flexible entry contract: a dag passes through (classed, or a 1.x
list with the dag shape), a \code{\link[=sniff_groups_lineage]{sniff_groups_lineage()}} object contributes its
\code{docs_per_group}, and a \code{docs_per_group} tibble is built into a dag.
}
\keyword{internal}
