% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_flow.R
\name{validate_flow}
\alias{validate_flow}
\title{Validate the trajectory flow contract}
\usage{
validate_flow(x)
}
\arguments{
\item{x}{The object to validate, typically a \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object
or the output of an alternative detector shaped like one.}
}
\value{
\code{x}, invisibly, when valid; otherwise an error listing every violation.
}
\description{
The authoritative checker of the flow contract (\code{trajectories}, \code{tree},
\code{graph}, \code{docs_per_group}, \code{last_year}). "Flow" names the object kind, not
the algorithm: any detector -- \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} today, alternative
\verb{sniff_trajectory_<algo>()} detectors in the future -- must return an object
that passes this validator and inherits the \code{birddog_flow} class.
}
\details{
Beyond the column schema, the checked invariants are the ones that otherwise
fail silently: every trajectory node label must be a \code{docs_per_group$group_id}
key and a \code{graph} vertex (the triple key documents and analyses share), the
\code{graph} must carry the \code{quantity_papers} / \code{prop_tracked_intra_group} vertex
attributes the line plots read, \code{absorbed_into} must reference existing
trajectories without forming a cycle, each final group must have exactly one
\code{central} present at \code{last_year}, central ids must use the \verb{tr::} convention,
and \code{start}/\code{end} must match the node years.
}
\seealso{
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=is_flow]{is_flow()}}

Other trajectory detection: 
\code{\link{is_flow}()},
\code{\link{sniff_trajectory_braid}()},
\code{\link{sniff_trajectory_channel}()},
\code{\link{sniff_trajectory_dag}()},
\code{\link{subset.birddog_flow}()}
}
\concept{trajectory detection}
