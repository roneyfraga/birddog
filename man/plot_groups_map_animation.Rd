% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_groups_map_animation.R
\name{plot_groups_map_animation}
\alias{plot_groups_map_animation}
\title{Animate the stock map: the citation network forming year by year}
\usage{
plot_groups_map_animation(
  groups_cumulative,
  labels_text = NULL,
  layout = "drl",
  seed = 888L,
  show_unassigned = TRUE,
  label = TRUE,
  label_size = 3.2,
  title_size = 14,
  point_size = 0.9
)
}
\arguments{
\item{groups_cumulative}{A stock object from \code{\link[=sniff_groups_cumulative]{sniff_groups_cumulative()}}.}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text}; \code{NULL}
(default) uses the bare group id. If no \code{id} matches any group, a warning is
issued and the group ids are used.}

\item{layout}{Force-directed layout passed to \code{\link[ggraph:ggraph]{ggraph::create_layout()}}
(default \code{"drl"}).}

\item{seed}{Integer seed for the stochastic layout (default \code{888L}).}

\item{show_unassigned}{Reveal nodes with no group (\code{NA}) as grey (default \code{TRUE}).}

\item{label}{Draw persistent group labels at the final centroids (default \code{TRUE}).}

\item{label_size}{Font size of the centroid labels (default \code{3.2}).}

\item{title_size}{Font size of the per-frame \code{"Year: ..."} title (default \code{14}).}

\item{point_size}{Node point size (default \code{0.9}).}
}
\value{
A \code{gganim} object (render with \code{\link[gganimate:animate]{gganimate::animate()}}).
}
\description{
One layout of the final network, with every paper revealed at the year it
first enters the cumulative network and held at its final position, so the
cloud grows without nodes moving. Returns a \code{gganim} object; render it to a
GIF/MP4 with \code{\link[gganimate:animate]{gganimate::animate()}} and \code{\link[gganimate:anim_save]{gganimate::anim_save()}}. Reveal and
colour reuse the static \code{\link[=plot_groups_map]{plot_groups_map()}} engine.
}
\details{
Rendering: the transition is one frame per year. Render with \code{duration} (not
\code{fps}), which works with either renderer: \code{gganimate::animate(anim, duration = 8)}
gives \code{nframes/duration} fps (with the \code{magick} renderer that ratio must divide
100, e.g. 32 years / 8 = 4). Save the rendered object, not the \code{gganim}:
\verb{a <- gganimate::animate(anim, duration = 8); gganimate::anim_save("x.gif", a)}.
Installing \code{gifski} removes the fps constraint.
}
\seealso{
\code{\link[=plot_groups_map]{plot_groups_map()}}, \code{\link[=plot_groups_map_interactive]{plot_groups_map_interactive()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
