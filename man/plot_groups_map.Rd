% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_groups_map.R
\name{plot_groups_map}
\alias{plot_groups_map}
\title{Plot the stock map: the complete citation network coloured by final group}
\usage{
plot_groups_map(
  groups_cumulative,
  labels_text = NULL,
  network_until = NULL,
  layout = "drl",
  seed = 888L,
  point_size = 0.9,
  point_alpha = 0.7,
  show_unassigned = TRUE,
  label = TRUE,
  label_size = 3.2,
  title = "Group map",
  title_size = 14,
  normalize = FALSE,
  normalize_q = 0.99
)
}
\arguments{
\item{groups_cumulative}{A stock object from \code{\link[=sniff_groups_cumulative]{sniff_groups_cumulative()}}; each
\verb{network_until_<year>} element must carry \verb{$network} (an igraph/tbl_graph
whose vertices have a \code{name} attribute) and \verb{$documents} (with columns
\code{name}, \code{group}).}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} (matching the group
ids, e.g. \code{"c1g1"}) and \code{text} (the human description). \code{NULL} (default)
uses the bare group id as the label. If no \code{id} matches any group, a warning
is issued and the group ids are used.}

\item{network_until}{Snapshot year to map. \code{NULL} (default) uses the last
element (the final groups).}

\item{layout}{Force-directed layout passed to \code{\link[ggraph:ggraph]{ggraph::create_layout()}}
(default \code{"drl"}, the OpenOrd layout, which separates communities into
legible territories on dense citation networks; \code{"fr"} and \code{"kk"} tend to
collapse a large network into a single hairball).}

\item{seed}{Integer seed for the stochastic layout (default \code{888L}).}

\item{point_size}{Node point size (default \code{0.9}).}

\item{point_alpha}{Node point alpha (default \code{0.7}).}

\item{show_unassigned}{Draw nodes with no group (\code{NA}) as grey background
(default \code{TRUE}); \code{FALSE} drops them.}

\item{label}{Draw the group text labels at centroids (default \code{TRUE}).}

\item{label_size}{Font size of the centroid labels (default \code{3.2}).}

\item{title}{Plot title: the default \code{"Group map"}, any string, or \code{NULL} for
no title.}

\item{title_size}{Title font size (default \code{14}).}

\item{normalize}{Rescale the layout for cross-year comparison (default
\code{FALSE}). When \code{TRUE}, coordinates are centred on the median and divided by
the robust radius (the \code{normalize_q} quantile of the distance to the centre),
then framed with a fixed unit window, so the dense core fills the plot
identically across snapshots. This equalises scale only, not orientation:
the force-directed layout assigns each graph an arbitrary rotation, so node
positions are not correspondable across years.}

\item{normalize_q}{Quantile of the radial distance kept inside the normalised
window (default \code{0.99}); only used when \code{normalize = TRUE}.}
}
\value{
A \code{ggplot} object.
}
\description{
The birddog stock map: every paper in the cumulative network is a point, laid
out by a force-directed layout of the citation network and coloured by its
final group, with a text label at each group's centroid. It is the stock
snapshot -- all final groups, the whole network at once -- positioned by
citation structure rather than text embeddings.
}
\seealso{
\code{\link[=sniff_groups_cumulative]{sniff_groups_cumulative()}}, \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
