% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_dag.R
\name{plot_trajectory_dag}
\alias{plot_trajectory_dag}
\title{Plot the soft trajectory DAG (all intermediate nodes)}
\usage{
plot_trajectory_dag(
  dag,
  color_by = c("terminal_group", "group"),
  edge_alpha = 0.25,
  point_size = 2.5,
  show_legend = FALSE,
  label_terminal = TRUE,
  labels_text = NULL,
  label_size = 3,
  title = "Cumulative network groups",
  legend_text_size = NULL,
  legend_ncol = NULL,
  axis_text_size = NULL
)
}
\arguments{
\item{dag}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object.}

\item{color_by}{How to colour the nodes (default \code{"terminal_group"}).
\code{"terminal_group"} uses the final group each node's lineage flows into (its
heaviest-successor destination), so a whole trajectory keeps one colour from
source to mouth and the colour legend has one entry per final group.
\code{"group"} uses the node's own per-year id (\code{c1gN}), which is a within-year
size rank rather than a stable identity, so the same trajectory changes
colour across years. Pick \code{"terminal_group"} to read how the final groups
formed, \code{"group"} to inspect the raw annual clustering labels.}

\item{edge_alpha}{Base edge transparency, scaled by Jaccard weight (default 0.25).}

\item{point_size}{Base node size (default 2.5).}

\item{show_legend}{Show the colour legend (default \code{FALSE}; many final groups
make a busy legend).}

\item{label_terminal}{Label the final-year nodes with their bare group id
(\code{c1gN}), to the right of each vertex, with a leader line back to the vertex
(default \code{TRUE}).}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text}. When
supplied, the node-colour legend moves to the bottom and each group is
relabelled \code{"c1gN: text"} (groups without a match keep the bare id); \code{NULL}
(default) leaves the legend governed by \code{show_legend}. The final-node labels
(\code{label_terminal}) are always the bare group id, independent of this.}

\item{label_size}{Font size of the final-node labels (default \code{3}).}

\item{title}{Plot title (default \code{"Cumulative network groups"}); \code{NULL} for
no title.}

\item{legend_text_size}{Font size of the colour-legend text; \code{NULL} (default)
keeps the theme default.}

\item{legend_ncol}{Number of columns in the colour legend; \code{NULL} (default)
lets ggplot choose.}

\item{axis_text_size}{Font size of the x-axis (year) tick labels; \code{NULL}
(default) keeps the theme default.}
}
\value{
A \code{ggplot} object.
}
\description{
The braided-river panorama: every cluster-year node laid out by year (x), with
succession edges, node colour = \code{terminal_group} (or \code{group}), node size =
paper count, and births ringed. Uses the year-aware Sugiyama layout from
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}'s graph. Nodes from dormant lineages that never reach
the final year (a colour value outside the final groups) are drawn grey and
kept out of the colour legend.
}
\details{
The visual encoding:
\itemize{
\item The \strong{x-axis is the publication year} (a Sugiyama layered layout); the
\strong{y-axis has no intrinsic meaning}, it only spreads nodes to reduce edge
crossings.
\item \strong{Node colour} is the trajectory basin (see \code{color_by}), \strong{node size} is
the paper count, and \strong{succession edges} fade with their Jaccard weight.
\item \strong{Births} (nodes with no strong predecessor) are ringed in black.
\item \strong{Dormant lineages} that never reach the final year are drawn grey and kept
out of the colour legend, so the colours stand for the final groups only.
\item With \code{label_terminal = TRUE} the final-year nodes are labelled \code{c1gN} to the
right (with a leader line); with \code{labels_text} the colour legend moves to the
bottom as \code{"c1gN: description"} entries.
}
}
\examples{
\dontrun{
# docs_per_group comes from sniff_groups_lineage()
dag <- sniff_trajectory_dag(docs_per_group)

# the whole braid, coloured by destination group
plot_trajectory_dag(dag)

# human-readable bottom legend, two columns, larger year labels
descr <- data.frame(id = c("c1g1", "c1g2"), text = c("Topic A", "Topic B"))
plot_trajectory_dag(dag, labels_text = descr, legend_ncol = 2, axis_text_size = 14)
}

}
\seealso{
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}, \code{\link[=plot_groups_per_year]{plot_groups_per_year()}},
\code{\link[=plot_trajectory_dag_interactive]{plot_trajectory_dag_interactive()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
