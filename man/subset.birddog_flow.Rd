% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset_birddog_flow.R
\name{subset.birddog_flow}
\alias{subset.birddog_flow}
\title{Subset a trajectory flow, preserving the contract}
\usage{
\method{subset}{birddog_flow}(x, subset, target = NULL, ...)
}
\arguments{
\item{x}{A \code{birddog_flow} object (or a subclass from a future detector).}

\item{subset}{Logical expression on the columns of \code{x$trajectories},
e.g. \code{size >= 50} or \code{type == "central"}.}

\item{target}{Optional character vector of \code{traj_id}s: keep each target and
its whole tributary subtree (the watershed). Intersects with \code{subset}.}

\item{...}{Ignored (base generic compatibility).}
}
\value{
An object of the same class as \code{x}, passing \code{\link[=validate_flow]{validate_flow()}}.
}
\description{
Filters the trajectories of a flow object while keeping it a valid flow:
\verb{$tree} and \code{absorbed_into} are reattached by transitive bypass (a kept
child points to its nearest kept ancestor; \code{NA} if none survives) and
\verb{$docs_per_group} follows the kept nodes. The result is a view, not a
census: the call is recorded in \code{attr(, "pruned")} and shown by \code{print()}.
Dispatches for any detector whose result inherits from \code{birddog_flow}
(the contract class), so future detection methods are subsettable for free.
}
\seealso{
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=validate_flow]{validate_flow()}}

Other trajectory detection: 
\code{\link{is_flow}()},
\code{\link{sniff_trajectory_braid}()},
\code{\link{sniff_trajectory_channel}()},
\code{\link{sniff_trajectory_dag}()},
\code{\link{validate_flow}()}
}
\concept{trajectory detection}
