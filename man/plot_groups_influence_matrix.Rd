% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_groups_influence_matrix.R
\name{plot_groups_influence_matrix}
\alias{plot_groups_influence_matrix}
\title{Plot the directed group-influence matrix (heatmap)}
\usage{
plot_groups_influence_matrix(
  influence,
  fill = c("surprise", "raw", "debt", "audience", "salton"),
  diagonal = c("mute", "show", "drop"),
  show_values = TRUE,
  label_size = 4,
  order_by = c("group", "balance"),
  axis_text_size = NULL,
  axis_title_size = NULL,
  x_angle = 0,
  legend_position = "right",
  bg = "white",
  title = NULL
)
}
\arguments{
\item{influence}{A \code{\link[=sniff_groups_influence]{sniff_groups_influence()}} object.}

\item{fill}{Which value to colour by: \code{"surprise"} (default, the flow against
the size null, on a diverging scale centred at \code{1}), \code{"raw"} (citation
counts), \code{"debt"}, \code{"audience"} or \code{"salton"}.}

\item{diagonal}{How to treat the intra-group cohesion cells: \code{"mute"}
(default, drawn grey, off the colour scale, so the between-group channels
keep the contrast), \code{"show"} (on the scale like any cell) or \code{"drop"} (not
drawn).}

\item{show_values}{Print the value inside each cell (default \code{TRUE}).}

\item{label_size}{Font size of the in-cell values (default \code{4}).}

\item{order_by}{Group order on both axes: \code{"group"} (default, \code{mixed_sort()})
or \code{"balance"} (sources first, sinks last).}

\item{axis_text_size, axis_title_size}{Font sizes of the tick labels and axis
titles; \code{NULL} (default) keeps the theme defaults.}

\item{x_angle}{Rotation in degrees of the column (cited-group) labels on the
top axis. Default \code{0} (horizontal); \code{90} stacks the \code{cNgN} ids vertically
so they stay legible on a small figure with many groups.}

\item{legend_position}{Legend placement: \code{"right"} (default), \code{"left"},
\code{"top"}, \code{"bottom"} or \code{"none"}.}

\item{bg}{Panel/plot background colour (default \code{"white"}).}

\item{title}{Plot title; \code{NULL} (default) removes it.}
}
\value{
A \code{ggplot} object.
}
\description{
\emph{Experimental.} The directed counterpart of \code{\link[=plot_trajectory_confluence_matrix]{plot_trajectory_confluence_matrix()}}:
a heatmap of the cross-citation channels from a \code{\link[=sniff_groups_influence]{sniff_groups_influence()}}
object. \strong{Rows are the citing group} ("is influenced by") and \strong{columns the
cited group} ("the influencer"), so a cell and its mirror across the diagonal
are the two directions of one pair. Confluence is symmetric; influence is not,
and that broken symmetry is the bidirectional relationship.
}
\examples{
\dontrun{
infl <- sniff_groups_influence(groups)
plot_groups_influence_matrix(infl)
plot_groups_influence_matrix(infl, fill = "debt", diagonal = "drop")
}

}
\seealso{
\code{\link[=sniff_groups_influence]{sniff_groups_influence()}}, \code{\link[=plot_groups_influence_network]{plot_groups_influence_network()}},
\code{\link[=plot_trajectory_confluence_matrix]{plot_trajectory_confluence_matrix()}}

Other visualization: 
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
