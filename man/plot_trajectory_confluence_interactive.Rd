% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_confluence_interactive.R
\name{plot_trajectory_confluence_interactive}
\alias{plot_trajectory_confluence_interactive}
\title{Interactive trajectory confluence (plotly): hover a stream for its trajectory}
\usage{
plot_trajectory_confluence_interactive(
  conf,
  target = "all",
  depth = NULL,
  min_n = 5,
  min_prop = 0.05,
  min_total_size = 0,
  min_duration_years = 0,
  width_range = c(0, 0.42),
  palette = NULL,
  labels_text = NULL,
  label_terminal = TRUE,
  label_size = 12,
  smooth = TRUE,
  title = NULL
)
}
\arguments{
\item{conf}{A \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}} object.}

\item{target}{What to draw, as in \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}} minus the focus
view: \code{"all"} (default) the whole forest, \code{"finals"} the central backbones,
\code{"intermediary"} the tributary streams, a single central \code{tr::cNgN} (its
feeders, coloured per tributary), or a vector of trajectory ids (centrals
stacked, or intermediates shown as their feeders).}

\item{depth, min_n, min_prop, min_total_size, min_duration_years, width_range, smooth}{Passed through to the same pruning / geometry helpers as
\code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}; see there for details.}

\item{palette}{Optional named colour vector overriding the default hues (keyed by
destination central in the all/multi view, else by tributary \code{traj_id}).}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text} mapping
ids to descriptions, appended to the hover (\verb{id: description ...}). Central
rivers are matched by their group id (\code{cNgN}), absorbed tributaries by their
own trajectory id (\code{trN}); \code{NULL} (default) uses the bare id.}

\item{label_terminal}{Name the central (or selected) rivers in a de-overlapped
column to the right, each joined to its stream by a leader line (default
\code{TRUE}).}

\item{label_size}{Font size (px) of those right-hand labels (default \code{12}).}

\item{title}{Plot title; \code{NULL} (default) derives one from \code{target}.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
The plotly twin of \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}. Every trajectory is a filled
stream over the publication year, laid out by the same centered lane packing and
coloured by the same rule (per destination central in the whole-forest / multi
view, per tributary in a single-central view). There is no legend: hovering a
\strong{central} river shows \code{tr::cNgN (size)} (plus its \code{labels_text} description),
and hovering a \strong{tributary} shows \code{trN (n/cohort -> tr::cNgN)}. With
\code{label_terminal = TRUE} the central (or selected) rivers are also named in a
de-overlapped column on the right, joined to their stream by a leader line.
Returns a \code{plotly} htmlwidget.
}
\details{
Unlike the static function this covers the \strong{forest views only} (\code{"all"},
\code{"finals"}, \code{"intermediary"}, one or more centrals, and an intermediate shown as
its upstream feeders); the single-intermediate \strong{focus view} and the faint
secondary-destination links are not drawn.

Built with plotly, following the same principles as
\code{\link[=plot_trajectory_dag_interactive]{plot_trajectory_dag_interactive()}}:
\itemize{
\item The \strong{x-axis is the publication year}; the \strong{y-axis has no intrinsic meaning}
(it only packs streams to minimise merge crossings).
\item Each stream is a filled polygon (\code{fill = "toself"}, hoverable over its area);
a tributary is \strong{grey} before the first paper it transfers, then in its colour.
\item \strong{There is no legend}: detail is revealed on hover, and only the terminal
(central or selected) rivers carry a printed label.
\item An intermediate that delivers \strong{0 documents} to the final it flows into is
always excluded (as in \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}).
}

The widget renders in the RStudio Viewer, embeds in Quarto/Shiny, or saves to a
standalone file with \code{htmlwidgets::saveWidget()}.
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
conf <- sniff_trajectory_confluence(flow)

plot_trajectory_confluence_interactive(conf, min_n = 20)

# one central's formation, hover the tributaries for their n/cohort
plot_trajectory_confluence_interactive(conf, target = "tr::c1g1")
}

}
\seealso{
\code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}, \code{\link[=plot_trajectory_dag_interactive]{plot_trajectory_dag_interactive()}},
\code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
