% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_dag_interactive.R
\name{plot_trajectory_dag_interactive}
\alias{plot_trajectory_dag_interactive}
\title{Interactive trajectory DAG (plotly): hover a node for its trajectory}
\usage{
plot_trajectory_dag_interactive(
  dag,
  labels_text = NULL,
  marker_range = c(4, 14),
  edge_alpha = 0.3,
  label_terminal = TRUE,
  label_size = 12,
  title = "Cumulative network groups"
)
}
\arguments{
\item{dag}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object.}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text} mapping
final group ids to descriptions, shown after the group id when hovering a
final-year node (\code{c1gN: description}); \code{NULL} (default) falls back to the
bare group id.}

\item{marker_range}{Min/max marker size in pixels; paper count is scaled into
this range (default \code{c(4, 14)}).}

\item{edge_alpha}{Edge opacity, \code{0}-\code{1} (default \code{0.3}).}

\item{label_terminal}{Show the final group id (\code{c1gN}) to the right of each
final-year node (default \code{TRUE}).}

\item{label_size}{Font size (px) of the final-node labels (default \code{12}).}

\item{title}{Plot title (default \code{"Cumulative network groups"}); \code{NULL} for
none.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
The plotly twin of \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}. Nodes are laid out by the same
year-aware Sugiyama layout, sized by paper count, and coloured by the final
group their lineage reaches. Dormant lineages that never reach the final year
are drawn grey. There is no legend: hovering a final-year node shows its group
id and \code{labels_text} description (\code{c1gN: description}), and hovering any other
(intermediate or dormant) node shows its raw id (e.g. \code{y2021c1g2}). When
\code{label_terminal} is \code{TRUE}, the
final group id (\code{c1gN}) is also printed to the right of each final-year node.
Returns a \code{plotly} htmlwidget.
}
\details{
The interactive twin of \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}, built with plotly:
\itemize{
\item The \strong{x-axis is the publication year} (Sugiyama layout); the \strong{y-axis has no
intrinsic meaning}.
\item \strong{Node colour} is the trajectory basin (the final group reached); \strong{dormant
lineages} that never reach the final year are grey. \strong{Node size} scales with
paper count over \code{marker_range}.
\item \strong{There is no legend.} Hovering a \strong{final-year node} shows \code{c1gN: description}
(its group id plus the \code{labels_text} description); hovering any \strong{other node}
shows its raw id (\code{yYYYYcXgN}).
\item \strong{Edges} are a single faint layer (\code{edge_alpha}) and are excluded from hover.
With \code{label_terminal = TRUE} the final group ids (\code{c1gN}) are printed in a
de-overlapped column to the right, each joined to its node by a leader line.
}

The widget renders in the RStudio Viewer, embeds in Quarto/Shiny, or saves to a
standalone file with \code{htmlwidgets::saveWidget()}.
}
\examples{
\dontrun{
# docs_per_group comes from sniff_groups_lineage()
dag <- sniff_trajectory_dag(docs_per_group)

plot_trajectory_dag_interactive(dag)

# hover the final nodes for descriptions; fainter edges
descr <- data.frame(id = c("c1g1", "c1g2"), text = c("Topic A", "Topic B"))
p <- plot_trajectory_dag_interactive(dag, labels_text = descr, edge_alpha = 0.2)
# htmlwidgets::saveWidget(p, "trajectory_dag.html", selfcontained = TRUE)
}

}
\seealso{
\code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}, \code{\link[=plot_groups_per_year]{plot_groups_per_year()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
