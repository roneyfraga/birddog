% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_dynamics_interactive.R
\name{plot_trajectory_dynamics_interactive}
\alias{plot_trajectory_dynamics_interactive}
\title{Interactive strategic map of trajectory dynamic states (plotly)}
\usage{
plot_trajectory_dynamics_interactive(
  dyn,
  target = c("finals", "intermediary"),
  thresholds = NULL,
  labels_text = NULL,
  label_id = FALSE,
  marker_range = c(6, 42),
  show_labels = FALSE,
  label_size = 11,
  palette = NULL,
  show_thresholds = TRUE,
  show_quadrants = TRUE,
  xlim = NULL,
  ylim = NULL,
  title = NULL
)
}
\arguments{
\item{dyn}{A \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} tibble.}

\item{target}{\code{"finals"} (default) maps the \strong{living (central)} trajectories
(novelty x growth), \code{"intermediary"} the \strong{declining (absorbed)} ones
(destination entropy x dormant share), as in \code{\link[=plot_trajectory_dynamics]{plot_trajectory_dynamics()}}.}

\item{thresholds}{The \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}-shaped list bounding the
regions and the reference lines. \code{NULL} (default) reuses the cuts attached to
\code{dyn} by \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}} (\code{attr(dyn, "state_thresholds")}), else
derives them via \code{\link[=data_state_thresholds]{data_state_thresholds()}}. Pass an explicit list to override.}

\item{labels_text}{Optional \code{data.frame} (\code{id}, \code{text}) mapping ids to
descriptions, used in the hover (and labels). The \code{"finals"} view matches by
group id (\code{cNgN}); the \code{"intermediaries"} view matches by trajectory id
(\code{trN}).}

\item{label_id}{Put the id on the label's own first line, above the
description (\code{cNgN} then the text for finals, \code{trN} then the text for
intermediaries), so the hover and pinned labels stay identifiable; the line
break is plotly's \verb{<br>}. Default \code{FALSE}.}

\item{marker_range}{Marker diameter range in px \code{c(min, max)} (default
\code{c(6, 42)}); area scales with the square root of \code{size}.}

\item{show_labels}{Pin a text label to each point (default \code{FALSE}; the detail
lives in the hover). Text may overlap when points are dense.}

\item{label_size}{Font size (px) of the pinned labels (default \code{11}).}

\item{palette}{Optional named colour vector overriding the default semantic
hues, keyed by state.}

\item{show_thresholds}{Draw the classification cut lines (default \code{TRUE}).}

\item{show_quadrants}{Shade and name the state regions (default \code{TRUE}).}

\item{xlim, ylim}{Optional \code{c(lo, hi)} axis ranges; \code{NULL} (default) expands to
keep every state region visible (the data, the threshold cuts, and the
\verb{[0,1]} share bounds, with padding).}

\item{title}{Plot title; \code{NULL} (default) derives one from \code{target}.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
The plotly twin of \code{\link[=plot_trajectory_dynamics]{plot_trajectory_dynamics()}}. The same quadrant map -- shaded
and named state regions, classification thresholds as reference lines, points
sized by community and coloured by state -- rendered as an HTML widget. Hovering
a point reveals the full indicator panel (growth, novelty, recruitment, inflow,
destination entropy, dormant share, and any CCT / hub-role columns), and the
state legend is clickable to isolate one state at a time.
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
dyn  <- sniff_trajectory_dynamics(flow)
plot_trajectory_dynamics_interactive(dyn, target = "finals", labels_text = descr)
}

}
\seealso{
\code{\link[=plot_trajectory_dynamics]{plot_trajectory_dynamics()}}, \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}},
\code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
