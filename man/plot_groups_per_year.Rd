% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_groups_per_year.R
\name{plot_groups_per_year}
\alias{plot_groups_per_year}
\title{Plot groups per year: the clustering fan converging to the final groups}
\usage{
plot_groups_per_year(
  dag,
  color_terminal = FALSE,
  point_size = 6,
  show_count = TRUE,
  count_size = 3.2,
  count_colour = "white",
  count_fill = "grey45",
  label_terminal = TRUE,
  label_size = 3,
  axis_text_size = NULL,
  title = "Groups per year"
)
}
\arguments{
\item{dag}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object.}

\item{color_terminal}{Colour the dots by \code{terminal_group} to foreshadow the
braid (default \code{FALSE}, a single neutral grey that reads as stock). The
colour legend is suppressed either way.}

\item{point_size}{Upper bound of the dot-size range; paper count is mapped to
\verb{[1, point_size]} (default \code{6}).}

\item{show_count}{Print the number of groups atop each year column, on a
filled label for emphasis (default \code{TRUE}).}

\item{count_size}{Font size of the per-year group-count text (default \code{3.2}).}

\item{count_colour}{Font colour of the count text (default \code{"white"}).}

\item{count_fill}{Background colour of the count label (default \code{"grey45"}, a
grey chip that makes the count stand out); use \code{"white"} for a plain box.}

\item{label_terminal}{Label the final-year nodes with their group id (\code{c1gN})
to the right of the last column, with a leader line (default \code{TRUE}).}

\item{label_size}{Font size of the final-node labels (default \code{3}).}

\item{axis_text_size}{Font size of the x-axis (year) tick labels; \code{NULL}
(default) keeps the theme default.}

\item{title}{Plot title (default \code{"Groups per year"}); \code{NULL} for none.}
}
\value{
A \code{ggplot} object.
}
\description{
The stock-side bridge view. One column per year; within a column the
cluster-year nodes are ranked by paper count with the largest at the baseline,
dot area encodes paper count, and (optionally) the number of groups that year
is printed atop the column. No succession edges, no birth rings -- it shows the
annual + cumulative clustering fan settling onto the final-year groups. It is
the stock-side counterpart of the flow-side \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}, and reads
the \code{sniff_trajectory_dag()} object directly.
}
\details{
The visual encoding:
\itemize{
\item \strong{One column per year} along the \strong{x-axis}; the \strong{y-axis has no intrinsic
meaning} (nodes are stacked by rank).
\item Within a column the cluster-year nodes are \strong{ranked by paper count} with the
largest at the baseline, and \strong{dot area encodes the paper count}.
\item With \code{show_count = TRUE} the \strong{number of groups that year} is printed atop
the column on a filled label (\code{count_fill} / \code{count_colour}).
\item There are \strong{no succession edges and no births}: this is the stock-side view
of how many groups exist each year and how large they are, settling onto the
final-year groups. For the flow (edges, trajectories) use
\code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}}.
\item With \code{label_terminal = TRUE} the final-year nodes are labelled \code{c1gN} to the
right of the last column.
}
}
\examples{
\dontrun{
# docs_per_group comes from sniff_groups_lineage()
dag <- sniff_trajectory_dag(docs_per_group)

plot_groups_per_year(dag)

# colour the dots by destination, bigger count chips and year labels
plot_groups_per_year(dag, color_terminal = TRUE, count_size = 5, axis_text_size = 14)
}

}
\seealso{
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}, \code{\link[=plot_trajectory_dag]{plot_trajectory_dag()}},
\code{\link[=plot_trajectory_dag_interactive]{plot_trajectory_dag_interactive()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
