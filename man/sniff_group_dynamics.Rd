% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_group_dynamics.R
\name{sniff_group_dynamics}
\alias{sniff_group_dynamics}
\title{Growth phase and formation of each final group}
\usage{
sniff_group_dynamics(
  docs_per_group,
  contribution = NULL,
  thresholds = NULL,
  growth_window = 3,
  last_year = NULL
)
}
\arguments{
\item{docs_per_group}{Per-year membership tibble (\code{group_id}, \code{document_id},
\code{network_until}, \code{group}).}

\item{contribution}{Optional \code{\link[=sniff_trajectory_group_contribution]{sniff_trajectory_group_contribution()}} output;
when given, \code{source_entropy}, \code{n_sources}, and \code{formation} are attached.}

\item{thresholds}{A \code{\link[=default_state_thresholds]{default_state_thresholds()}}-shaped list (default).}

\item{growth_window}{Production-curve points over which \code{recent_growth} is
measured (default 3).}

\item{last_year}{Final year (default: max \code{network_until}).}
}
\value{
A tibble, one row per final group, sorted by descending \code{recent_growth}:
\code{group}, \code{size}, \code{start}, \code{end}, \code{span}, \code{median_pub_year}, \code{recent_growth},
\code{phase}, and (with \code{contribution}) \code{source_entropy}, \code{n_sources}, \code{formation}.
}
\description{
For each final-year group, builds its production curve (cumulative count of its
current members by their first-appearance year) and classifies the growth
\code{phase} (emergence / maturity / dormancy). The convergent-vs-divergent character
of a group is its \code{formation}, read from the bipartite \code{source_entropy} when a
\code{\link[=sniff_trajectory_group_contribution]{sniff_trajectory_group_contribution()}} object is supplied (low normalized
entropy = convergent, high = divergent).
}
\seealso{
\code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}}, \code{\link[=sniff_trajectory_group_contribution]{sniff_trajectory_group_contribution()}}
}
