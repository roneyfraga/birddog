% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/soft_trajectory_dag.R
\name{.build_coupling_edges}
\alias{.build_coupling_edges}
\title{Build the global temporal edge table by bibliographic coupling}
\usage{
.build_coupling_edges(
  docs_per_group,
  references,
  min_group_size = 10,
  jaccard_min = 0.05,
  k_out = 2
)
}
\arguments{
\item{docs_per_group}{Tibble with \code{group_id} and \code{document_id}.}

\item{references}{Data frame with \code{document_id} and \code{feature} (one cited
reference per row).}

\item{min_group_size}{Minimum distinct-document count for a node (default 10).}

\item{jaccard_min}{Minimum Jaccard weight to keep an edge (default 0.05).}

\item{k_out}{Max outgoing edges kept per source node (default 2).}
}
\value{
Tibble with \code{from}, \code{to}, \code{weight}, \code{documents} (here \code{documents} is
the count of shared references, the tie-break analogue of the overlap count).
}
\description{
The coupling twin of \code{\link[=.build_global_edges]{.build_global_edges()}}: consecutive-year edges weighted
by the Jaccard of two nodes' cited-reference sets (the references their
documents cite) instead of the Jaccard of their shared documents. Same filters:
\code{weight >= jaccard_min}, consecutive years only, top \code{k_out} outgoing edges per
source. Two nodes can couple without sharing a single document.
}
\keyword{internal}
