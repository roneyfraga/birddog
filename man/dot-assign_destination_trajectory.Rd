% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_destination.R
\name{.assign_destination_trajectory}
\alias{.assign_destination_trajectory}
\title{Attribute cohort papers to the trajectory that carries them forward}
\usage{
.assign_destination_trajectory(
  cohort,
  terminal_year,
  all_detected,
  docs_per_group,
  exclude_key = NULL
)
}
\arguments{
\item{cohort}{Character vector of document ids.}

\item{terminal_year}{Death year of the source trajectory.}

\item{all_detected}{Named list of \code{\link[=detect_main_trajectories]{detect_main_trajectories()}} outputs.}

\item{docs_per_group}{Per-year membership tibble.}

\item{exclude_key}{Optional trajectory key(s) (\code{"group::traj_id"}) to skip.}
}
\value{
Tibble \code{document_id}, \code{dest_traj_key}, \code{dest_group}, \code{dest_traj_id},
with \code{"(none)"} for papers that no trajectory carries past the death year.
}
\description{
For each cohort paper, finds the trajectory (across all groups) whose nodes
contain it in the most years after the death year. The source trajectory,
whose nodes all fall on or before the death year, is excluded automatically.
}
\keyword{internal}
