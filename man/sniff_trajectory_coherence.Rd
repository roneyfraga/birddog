% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_coherence.R
\name{sniff_trajectory_coherence}
\alias{sniff_trajectory_coherence}
\title{Appraise the content coherence of a trajectory flow}
\usage{
sniff_trajectory_coherence(
  flow,
  content,
  profile = "incremental",
  signals = names(content)
)
}
\arguments{
\item{flow}{A \code{birddog_flow} (from \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} or
\code{\link[=sniff_trajectory_channel]{sniff_trajectory_channel()}}).}

\item{content}{Named list; each element a data frame with columns
\code{document_id} and \code{feature} (long, one row per document-feature pair). The
element name labels the signal in the output (e.g. \code{coupling}, \code{keywords}).}

\item{profile}{\code{"incremental"} (default: profile a node from the papers new to
its year, stripping cumulative carry-over) and/or \code{"full"} (all of a node's
papers).}

\item{signals}{Which \code{names(content)} to score (default all).}
}
\value{
An \code{sniff_trajectory_coherence} object: a list with \code{nodes} (one row per
node x signal x profile x resolution) and \code{summary} (one row per
signal x profile x resolution, with \code{mean_sil}, \code{n_nodes}, \code{n_singletons},
\code{n_excluded}, \code{coverage}).
}
\description{
Scores how content-coherent a \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} / \code{\link[=sniff_trajectory_channel]{sniff_trajectory_channel()}}
partition is, using a silhouette over an independent content signal (shared
references or keywords) the detectors never optimized. Each group-year node is
profiled from its documents' features; node-to-node distance is \verb{1 - Salton cosine}; the silhouette is computed at two cluster resolutions: \code{trajectory}
(each backbone/tributary chain) and \code{final_group} (each central + its tributaries).
}
\seealso{
\code{\link[=sniff_trajectory_comparison]{sniff_trajectory_comparison()}}, \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=sniff_trajectory_channel]{sniff_trajectory_channel()}},
\code{\link[=validate_flow]{validate_flow()}}

Other flow utilities: 
\code{\link{sniff_trajectory_comparison}()}
}
\concept{flow utilities}
