% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_group_contribution.R
\name{sniff_trajectory_group_contribution}
\alias{sniff_trajectory_group_contribution}
\title{Trajectory-to-group contribution as a bipartite network}
\usage{
sniff_trajectory_group_contribution(detected, docs_per_group = NULL)
}
\arguments{
\item{detected}{A \code{\link[=detect_global_trajectories]{detect_global_trajectories()}} object (its \code{trajectories}
tibble must carry a \code{terminal_group} column and globally-unique \code{traj_id}).}

\item{docs_per_group}{Per-year membership tibble (\code{group_id}, \code{document_id},
\code{network_until}, \code{group}). Defaults to \code{detected$docs_per_group}.}
}
\value{
A list:
\describe{
\item{long}{Tidy tibble \code{traj_id}, \code{terminal_group}, \code{group_final},
\code{n_shared}, \code{prop_of_group}, \code{prop_of_traj}.}
\item{incidence}{Integer matrix \code{B} (trajectories x final groups), weight =
\code{n_shared}; ready for \code{igraph::graph_from_biadjacency_matrix()} or the
\code{bipartite} package.}
\item{graph}{Weighted bipartite igraph (rows = trajectories \code{type = FALSE},
columns = groups \code{type = TRUE}).}
\item{groups}{Per final group: \code{n_sources}, \code{source_entropy} (low =
convergent formation, high = divergent), \code{dominant_traj},
\code{dominant_traj_share}.}
\item{trajectories}{Per trajectory: \code{n_groups}, \code{reach} (weighted degree =
papers placed in final groups), \code{dominant_group}, \code{dominant_group_share}.}
\item{last_year}{The final year used.}
}
}
\description{
For a \code{\link[=detect_global_trajectories]{detect_global_trajectories()}} object, computes how much each global
trajectory contributes to each final-year group. The result is the
trajectory x group incidence matrix \code{B} (weight = shared papers), the bipartite
igraph, the tidy long table, and Layer-1 node indicators. Nestedness and
bipartite modularity are intentionally left to the user's \code{bipartite} package
tooling, which runs directly on \code{B} / the graph.
}
\details{
For trajectory \code{tr} (papers = the union of documents across its nodes) and
final group \code{g} (documents in \code{g}'s last-year cluster):
\verb{n_shared = |papers(tr) ∩ final(g)|},
\verb{prop_of_group = n_shared / |final(g)|} (how much of \code{g} came from \code{tr}),
\verb{prop_of_traj = n_shared / |papers(tr)|} (where \code{tr}'s papers ended up).
Because trajectories share papers, the columns of \code{B} do not sum to one; these
are overlapping contributions, not a partition.
}
\seealso{
\code{\link[=detect_global_trajectories]{detect_global_trajectories()}}, \code{\link[=sniff_trajectory_projection]{sniff_trajectory_projection()}},
\code{\link[=plot_trajectory_group_bipartite]{plot_trajectory_group_bipartite()}}
}
