% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_confluence_matrix.R
\name{plot_trajectory_confluence_matrix}
\alias{plot_trajectory_confluence_matrix}
\title{Plot the trajectory confluence matrix (finals vs intermediates)}
\usage{
plot_trajectory_confluence_matrix(
  flow,
  conf = NULL,
  fill = c("n_shared", "prop_of_group", "prop_of_traj"),
  orientation = c("finals-cols", "finals-rows"),
  order_by = c("terminal", "reach", "size", "alpha", "cluster"),
  min_n = 5,
  min_prop = 0.05,
  min_total_size = 0,
  min_duration_years = 0,
  min_target_n = 0,
  mark_terminal = TRUE,
  show_values = FALSE,
  label_size = 2.5,
  axis_text_size = NULL,
  axis_title_size = NULL,
  legend_position = "right",
  legend_text_size = NULL,
  bg = "white",
  title = "Trajectory matrix"
)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object.}

\item{conf}{An optional precomputed \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}} object for
\code{flow}; \code{NULL} (default) computes it.}

\item{fill}{\code{"n_shared"} (default, log-scaled), \code{"prop_of_group"}, or
\code{"prop_of_traj"}.}

\item{orientation}{\code{"finals-cols"} (default): final trajectories \code{tr::cNgN} on
the rows (y-axis), intermediate trajectories \code{trN} on the columns (x-axis) --
the "finals (rows) x intermediates (cols)" matrix. \code{"finals-rows"} is the
transpose (finals on the columns, intermediates on the rows).}

\item{order_by}{How the intermediate trajectories are ordered along their axis:
\code{"terminal"} (default, grouped by dominant final then reach), \code{"reach"} (total
shared documents), \code{"size"} (trajectory size at handoff), \code{"alpha"}
(alphabetical), or \code{"cluster"} (seriation that reorders \strong{both} axes by
hierarchical clustering -- intermediates by their destination profile and
finals by their source profile -- so alike rows and columns form blocks).}

\item{min_n, min_prop, min_total_size, min_duration_years}{Thresholds selecting
which intermediate trajectories are shown, passed to the same prune as
\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}: an intermediate is kept only if it transfers
\verb{>= min_n} papers and \verb{>= min_prop} of its cohort, has total size
\verb{>= min_total_size} and lifespan \verb{>= min_duration_years}.}

\item{min_target_n}{Minimum shared documents a cell must have to be drawn
(default \code{0}, all cells); raise it to hide the long tail of small overlaps.
Cells with zero shared documents are never drawn. Final trajectories are
always kept on their axis, even when the thresholds leave them with no cell.}

\item{mark_terminal}{Outline each intermediate's dominant-final cell
(default \code{TRUE}).}

\item{show_values}{Print the value inside each cell (default \code{FALSE}). The text
colour auto-contrasts (dark on light cells, light on dark) for legibility.}

\item{label_size}{Font size of the in-cell values (default \code{2.5}).}

\item{axis_text_size}{Font size of the axis (trajectory id) tick labels; \code{NULL}
(default) keeps the theme default.}

\item{axis_title_size}{Font size of the axis titles ("Final Trajectory" /
"Intermediate Trajectory"); \code{NULL} (default) keeps the theme default.}

\item{legend_position}{Where to put the colourbar: \code{"right"} (default),
\code{"left"}, \code{"top"}, \code{"bottom"}, \code{"none"}, or a \code{c(x, y)} coordinate inside the
panel (each in \verb{0..1}). \code{"top"}/\code{"bottom"} draw the colourbar horizontally.}

\item{legend_text_size}{Font size of the legend text; \code{NULL} (default) keeps the
theme default.}

\item{bg}{Panel/plot background colour (default \code{"white"}). A darker background
(e.g. \code{"grey20"}) raises contrast against the bright cells; the grid lines and
the terminal outline auto-adapt to its luminance.}

\item{title}{Plot title (default \code{"Trajectory matrix"}); \code{NULL} removes it.}
}
\value{
A \code{ggplot} object.
}
\description{
A heatmap of how each intermediate (absorbed) trajectory's papers spread across
the final trajectories. By default \strong{rows are the final trajectories} \code{tr::cNgN}
and \strong{columns are the intermediate trajectories} \code{trN} (swap with
\code{orientation}); the fill is the coupling strength (shared documents) and each
intermediate's dominant-final cell is outlined. Which intermediates appear is
decided by the same pruning the other flow plots use
(\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}, \code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}), so the column
set is consistent across views.
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
plot_trajectory_confluence_matrix(flow, min_n = 20, show_values = TRUE)
plot_trajectory_confluence_matrix(flow, order_by = "cluster", bg = "grey20")
}

}
\seealso{
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}},
\code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()},
\code{\link{plot_trajectory_lines_3d}()}
}
\concept{visualization}
