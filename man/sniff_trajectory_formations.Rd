% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_formations.R
\name{sniff_trajectory_formations}
\alias{sniff_trajectory_formations}
\title{Precompute the formation of every trajectory in one pass}
\usage{
sniff_trajectory_formations(all_detected, docs_per_group, last_year = NULL)
}
\arguments{
\item{all_detected}{Named list of \code{\link[=detect_main_trajectories]{detect_main_trajectories()}} outputs, one
per group, keyed by group id.}

\item{docs_per_group}{Per-year membership tibble, as in
\code{groups_cumulative_trajectories$docs_per_group} (columns \code{group_id},
\code{document_id}, \code{network_until}, \code{group}).}

\item{last_year}{Final year (default: the maximum \code{network_until}).}
}
\value{
A named list keyed by target trajectory (\code{"group::traj_id"}), one
entry per target that has at least one feeder. Each entry has the same shape
as \code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}} output (\code{target}, \code{target_info},
\code{feeders}, \code{total_inflow}, \code{last_year}), with all feeders kept
(\code{kept = TRUE}); re-apply thresholds downstream by overwriting \code{kept} before
\code{\link[=plot_trajectory_formation_2d]{plot_trajectory_formation_2d()}}.
}
\description{
Batch version of \code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}}. A single
\code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}} call already walks every trajectory that
stops, so collecting the feeders of \emph{all} targets at once costs essentially
the same. Use it to precompute results offline (e.g. for a Shiny app) so the
server only loads and plots, never recomputes.
}
\examples{
\dontrun{
fs <- sniff_trajectory_formations(
  groups_detected_trajectories,
  groups_cumulative_trajectories$docs_per_group
)
saveRDS(fs, "rawfiles/trajectory_formations.rds")
plot_trajectory_formation_2d(fs[["c1g10::tr1"]])
}

}
\seealso{
\code{\link[=sniff_trajectory_formation]{sniff_trajectory_formation()}}, \code{\link[=sniff_trajectory_self_sufficiency]{sniff_trajectory_self_sufficiency()}}
}
