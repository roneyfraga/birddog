% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_trajectory_lines_3d.R
\name{plot_trajectory_lines_3d}
\alias{plot_trajectory_lines_3d}
\title{Plot the global trajectories as 3D variable-width lines}
\usage{
plot_trajectory_lines_3d(
  flow,
  target = "all",
  conf = NULL,
  min_n = 5,
  min_prop = 0.05,
  min_total_size = 0,
  min_duration_years = 0,
  min_target_n = 0,
  width_range = c(4, 12),
  use_raw_papers = FALSE,
  log_scale = FALSE,
  label_size = 16,
  hover_font_size = 12,
  dest_min_prop = 0.05,
  lowlight_alpha = 0.75,
  lowlight_color = "#9AA5B1",
  lowlight_width = 3,
  show_background = TRUE,
  year_range = NULL,
  palette = NULL,
  title = NULL,
  legend_position = c("bottom", "right", "top", "left", "none"),
  legend_ncol = NULL,
  labels_text = NULL
)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object, or a \code{docs_per_group} tibble /
\code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object the flow is built from.}

\item{target}{What to draw. \code{"all"} (default) draws every central and the
trajectories feeding it; a central \code{traj_id} (e.g. \code{"tr::c1g1"}) or a vector
of them draws only those finals and their feeders.}

\item{conf}{An optional precomputed \code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}} object for
\code{flow}; pass it to skip recomputing the confluence on every call. \code{NULL}
(default) computes it.}

\item{min_n, min_prop, min_total_size, min_duration_years}{Feeder thresholds,
passed to the same pruning used by \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}: an
intermediate is drawn only if it transfers \verb{>= min_n} papers and \verb{>= min_prop}
of its cohort, has total size \verb{>= min_total_size} and lifespan
\verb{>= min_duration_years}; a pruned intermediate takes its own feeders with it.}

\item{min_target_n}{Minimum papers an intermediate must leave in the selected
final(s) by the last year to be drawn (default \code{0}, no filter). Only applies
to a targeted final view.}

\item{width_range}{Line-width range \code{c(min, max)} (default \code{c(4, 12)}), scaled
per trajectory by its cumulative tracked documents.}

\item{use_raw_papers}{Scale z and width by raw \code{quantity_papers} (\code{TRUE}) or by
the weighted \code{quantity_papers * prop_tracked_intra_group} (\code{FALSE}, default).}

\item{log_scale}{Apply \code{log1p()} to the z-axis (cumulative documents), which
keeps large trajectories from dwarfing small ones (default \code{FALSE}).}

\item{label_size}{Font size of the \code{trN} / \code{tr::cNgN} end labels (default \code{16}).}

\item{hover_font_size}{Font size for hover tooltips (default \code{12}).}

\item{dest_min_prop}{In the \code{target = "all"} and single-target views, the
minimum share of an intermediate's terminal cohort that must reach a final
for that final to appear in its legend split (default \code{0.05}). In the
single-target view the targeted final is always kept regardless of this
threshold.}

\item{lowlight_alpha, lowlight_color, lowlight_width}{Opacity, colour and width of
the light-grey links between trajectories.}

\item{show_background}{Draw the light-grey links between trajectories -- the
edges of the DAG that are not on any single trajectory's path (the hand-offs
and forks), at the nodes' real heights, as in \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}
(default \code{TRUE}).}

\item{year_range}{Optional \code{c(from, to)} years fixing the x-axis range, so a
short trajectory can be shown on the same window as a long one. \code{NULL}
(default) fits the drawn trajectories.}

\item{palette}{Optional named colour vector overriding the default hues (keyed
by trajectory \code{traj_id}).}

\item{title}{Plot title; \code{NULL} (default) derives one from \code{target}.}

\item{legend_position}{Where to place the legend: \code{"bottom"} (default, matching
\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}), \code{"right"}, \code{"top"}, \code{"left"}, or \code{"none"} to
hide it.}

\item{legend_ncol}{Number of columns in the legend (default \code{NULL}, plotly
chooses). Applies to the horizontal legends (\code{"bottom"} / \code{"top"}): each
entry is given \code{1 / legend_ncol} of the width, so \code{legend_ncol} entries fit
per row. Ignored for the vertical legends (\code{"right"} / \code{"left"}).}

\item{labels_text}{Optional \code{data.frame} with columns \code{id} and \code{text} mapping
group ids (e.g. \code{"c1g1"}) to descriptions, as in
\code{\link[=plot_trajectory_confluence]{plot_trajectory_confluence()}}. When supplied, hovering a trajectory's nodes
shows the description of its group beneath the feeding legend. \code{NULL}
(default) shows the bare group ids.}
}
\value{
A plotly 3D plot object.
}
\description{
The 3D counterpart of \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}. Every global trajectory
is drawn over the
year-aware Sugiyama layout as a growing-thickness line: \strong{x = publication
year}, \strong{y = route} (the Sugiyama branching coordinate that separates
trajectories), and \strong{z = cumulative tracked documents} along the path. The
central finals \code{tr::cNgN} and the intermediate \code{trN} that feed them share one
colour-per-trajectory scheme; the legend reports, for every intermediate, how
many papers of its terminal cohort reached each final (a single \code{trN} can feed
more than one final, which the legend makes explicit). Selection and thresholds
are identical to \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}; only the rendering adds the
document z-axis and interactive hover.
}
\details{
\itemize{
\item \strong{z = cumulative tracked documents} along each trajectory (raw or weighted),
accumulated node by node; \code{log_scale = TRUE} compresses it.
\item \strong{Line width} grows per trajectory with the same cumulative measure, so each
line is thinnest at its birth and thickest at its terminal node (the width is
rescaled within each trajectory, matching \code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}).
\item A single-year \strong{central} (e.g. a final-year-only community like \code{tr::c1g17})
has no segment to draw and is rendered as a terminal ball; a single-year
\strong{feeder} is dropped (it would show a label with no visible line).
\item \strong{Colour} is per trajectory and the legend carries the feeding split: a
central reads \code{tr::cNgN (final, n)} (n = documents that remained in its
final-year community), an intermediate reads \verb{trN -> c1g1:n, c1g2:n} (its
terminal cohort split across finals). In a single-target view it reads
\verb{trN: p\% (n) -> c1g15:n, c1g1:n, ...} -- \verb{p\%} and the parenthesised \code{(n)} are
the share and the document count that reached the target (as in
\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}); the split then shows the feeder's full
destination, so a feeder of \code{c1g1} reveals that more of it may flow elsewhere.
\item \strong{Hovering a node} shows that same feeding-legend text, the trajectory's
\code{labels_text} description (when supplied), the node's year, and the absolute
documents in that cluster-year (\code{quantity_papers}, not the running cumulative
that sets the height).
}
}
\examples{
\dontrun{
flow <- sniff_trajectory_braid(docs_per_group)
plot_trajectory_lines_3d(flow, min_n = 20)
plot_trajectory_lines_3d(flow, target = "tr::c1g1")
}

}
\seealso{
\code{\link[=plot_trajectory_lines_2d]{plot_trajectory_lines_2d()}}, \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}},
\code{\link[=sniff_trajectory_confluence]{sniff_trajectory_confluence()}}

Other visualization: 
\code{\link{plot_groups_influence_matrix}()},
\code{\link{plot_groups_influence_network}()},
\code{\link{plot_groups_lineage_2d}()},
\code{\link{plot_groups_lineage_3d}()},
\code{\link{plot_groups_map}()},
\code{\link{plot_groups_map_animation}()},
\code{\link{plot_groups_map_interactive}()},
\code{\link{plot_groups_per_year}()},
\code{\link{plot_trajectory_confluence}()},
\code{\link{plot_trajectory_confluence_interactive}()},
\code{\link{plot_trajectory_confluence_matrix}()},
\code{\link{plot_trajectory_dag}()},
\code{\link{plot_trajectory_dag_interactive}()},
\code{\link{plot_trajectory_dispersion}()},
\code{\link{plot_trajectory_dynamics}()},
\code{\link{plot_trajectory_dynamics_interactive}()},
\code{\link{plot_trajectory_formation}()},
\code{\link{plot_trajectory_lines_2d}()}
}
\concept{visualization}
