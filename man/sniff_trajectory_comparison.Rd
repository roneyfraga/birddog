% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_comparison.R
\name{sniff_trajectory_comparison}
\alias{sniff_trajectory_comparison}
\title{Compare trajectory detectors by content coherence}
\usage{
sniff_trajectory_comparison(
  flows,
  content,
  profile = "incremental",
  signals = names(content)
)
}
\arguments{
\item{flows}{Named list of \code{birddog_flow} objects, e.g.
\code{list(flow = ..., channel = ...)}.}

\item{content}{Named list of \verb{(document_id, feature)} data frames (see
\code{\link[=sniff_trajectory_coherence]{sniff_trajectory_coherence()}}).}

\item{profile}{Passed to \code{\link[=sniff_trajectory_coherence]{sniff_trajectory_coherence()}} (default \code{"incremental"}).}

\item{signals}{Which \code{names(content)} to score (default all).}
}
\value{
A \code{sniff_trajectory_comparison} object: a list with \code{summary} (per
method x signal x profile x resolution), \code{verdict} (\code{best_method}, \code{delta}
per signal x profile x resolution), \code{agreement} (do all signals pick the same
\code{best_method}, per profile x resolution), and \code{contested} (nodes the
detectors place in different final groups, with each method's \code{final_group}
and silhouette, plus the \code{closer_method};
\code{NULL} unless exactly two flows are given).
}
\description{
Runs \code{\link[=sniff_trajectory_coherence]{sniff_trajectory_coherence()}} on each named flow, across every content signal and the
chosen \code{profile}(s), and reports which detector produces the more
content-coherent partition, whether the signals agree, and the contested nodes
(those a pair of detectors place in different final groups).
}
\seealso{
\code{\link[=sniff_trajectory_coherence]{sniff_trajectory_coherence()}}

Other flow utilities: 
\code{\link{sniff_trajectory_coherence}()}
}
\concept{flow utilities}
