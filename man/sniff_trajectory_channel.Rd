% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_channel.R
\name{sniff_trajectory_channel}
\alias{sniff_trajectory_channel}
\title{Detect channel trajectories by global optimal-path routing}
\usage{
sniff_trajectory_channel(x, min_group_size = 10, jaccard_min = 0.05, k_out = 2)
}
\arguments{
\item{x}{A \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} object, a \code{\link[=sniff_groups_lineage]{sniff_groups_lineage()}} object,
or a \code{docs_per_group} tibble (the DAG is built internally).}

\item{min_group_size, jaccard_min, k_out}{Passed to \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}} when
\code{x} is not already a DAG object.}
}
\value{
A \code{birddog_flow} object (see \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}) with the extra
\code{latest_departure} column.
}
\description{
An alternative to \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}. Instead of flow's local
mutual-dominance trunk edges, each node is routed to the successor on its
globally cheapest path to a final-year node, where edge cost is
\code{-log(weight)} (the Jaccard). The minimum-cost path is the maximum-product
Jaccard path (highest geometric-mean coherence). Final-year nodes root one
in-tree each (the watershed of a final community); the cheapest birth -> final
chain is the \strong{central} trajectory (\verb{tr::<group>}), the rest are \strong{absorbed}
tributaries (\verb{tr1 … trN}), mirroring the flow object exactly. "Flow" names the
object kind, not the algorithm, so the result is a \code{birddog_flow} and passes
\code{\link[=validate_flow]{validate_flow()}}.
}
\details{
Adds one column to \code{trajectories}: \code{latest_departure}, the year of the most
recent birth that can reach the trajectory's tail by any time-respecting path
(Wu et al. 2014 latest-departure). For a central this is the freshest input
anywhere in the final community's ancestry; combined with \code{start} it separates
old-but-freshly-fed communities from genuinely young ones.
}
\references{
Wu H, Cheng J, Huang S, Ke Y, Lu Y, Xu Y (2014). Path Problems in
Temporal Graphs. Proc. VLDB Endowment 7(9).
}
\seealso{
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=validate_flow]{validate_flow()}}, \code{\link[=sniff_trajectory_dag]{sniff_trajectory_dag()}}

Other trajectory detection: 
\code{\link{is_flow}()},
\code{\link{sniff_trajectory_braid}()},
\code{\link{sniff_trajectory_dag}()},
\code{\link{subset.birddog_flow}()},
\code{\link{validate_flow}()}
}
\concept{trajectory detection}
