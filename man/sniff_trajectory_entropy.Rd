% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_entropy.R
\name{sniff_trajectory_entropy}
\alias{sniff_trajectory_entropy}
\title{Per-year keyword diversity (Pielou's J') along each flow trajectory}
\usage{
sniff_trajectory_entropy(flow, keywords)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object.}

\item{keywords}{A long data frame with columns \code{document_id} and \code{keyword}, one
row per document-keyword pair (e.g. the corpus \code{DE} field split on \verb{;} and
keyed by the membership id).}
}
\value{
A tibble, one row per trajectory: \code{traj_id}, \code{type}, \code{group}, and
\code{keyword_entropy}, a list-column whose each cell is a tibble with columns
\code{year} and \code{keyword_entropy} (Pielou's J' over that node-year's documents, in
\verb{[0, 1]}; \code{0} for a single keyword, \code{NA} when the year's documents carry none).
}
\description{
Recomputes, from the documents in each \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} trajectory's
own group-year nodes, the normalized Shannon entropy (Pielou's evenness J') of
their keyword distribution, one value per year of the trajectory's life. A
falling series marks thematic convergence, a rising one diversification. Unlike
\code{\link[=sniff_entropy]{sniff_entropy()}}, which scores a static \code{sniff_groups()} partition by
group-year, this follows the trajectory's actual membership, so an absorbed
tributary is scored on its own documents rather than its destination group's.
}
\details{
This is \strong{keyword} entropy (thematic diversity), distinct from the dynamics
\code{dest_entropy} (the dispersion of an absorbed cohort across destination groups);
the dynamics column is named \code{keyword_entropy} to keep the two apart.
}
\seealso{
\code{\link[=sniff_entropy]{sniff_entropy()}}, \code{\link[=sniff_trajectory_cct]{sniff_trajectory_cct()}}, \code{\link[=sniff_trajectory_dynamics]{sniff_trajectory_dynamics()}},
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}

Other trajectory analysis: 
\code{\link{sniff_trajectory_cct}()},
\code{\link{sniff_trajectory_community}()},
\code{\link{sniff_trajectory_confluence}()},
\code{\link{sniff_trajectory_contribution}()},
\code{\link{sniff_trajectory_destination}()},
\code{\link{sniff_trajectory_dynamics}()},
\code{\link{sniff_trajectory_emergence_owners}()},
\code{\link{sniff_trajectory_formation}()},
\code{\link{sniff_trajectory_hubs}()},
\code{\link{sniff_trajectory_self_sufficiency}()}
}
\concept{trajectory analysis}
