% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_trajectory_dynamics.R
\name{sniff_trajectory_dynamics}
\alias{sniff_trajectory_dynamics}
\title{Dynamic-state indicators and classification for flow trajectories}
\usage{
sniff_trajectory_dynamics(
  flow,
  thresholds = NULL,
  k = 1,
  growth_window = 3,
  novelty_window = 5,
  winsorize = 3,
  cct = NULL,
  entropy = NULL,
  hubs = NULL
)
}
\arguments{
\item{flow}{A \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} object.}

\item{thresholds}{A \code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}-shaped list. \code{NULL} (default)
\strong{derives the cut points from the data} via \code{\link[=data_state_thresholds]{data_state_thresholds()}} with
the \code{k} below, so the classification adapts to the dataset; pass
\code{fixed_state_thresholds()} to force the fixed constants instead. The
thresholds actually used are attached to the result as
\code{attr(result, "state_thresholds")}, which the plots read back.}

\item{k}{Robust deviations for the data-driven thresholds when \code{thresholds}
is \code{NULL} (default 1); see \code{\link[=data_state_thresholds]{data_state_thresholds()}}. Ignored when an explicit
\code{thresholds} list is supplied.}

\item{growth_window}{Curve points over which \code{growth_rate} is fitted (default 3).}

\item{novelty_window}{Years counted as recent for \code{novelty} (default 5).}

\item{winsorize}{Cap each pillar's robust z-score at \code{c(-winsorize, +winsorize)}
before summing into \code{emergence_index}, so no single extreme pillar (e.g. a
young core 18 MADs out on growth) dominates the ranking. Default \code{3} (near the
Iglewicz--Hoaglin modified-z outlier boundary of 3.5); \code{NULL} leaves the robust
z-scores uncapped.}

\item{cct}{Optional \code{\link[=sniff_trajectory_cct]{sniff_trajectory_cct()}} output (one row per \code{traj_id}),
left-joined to add the \code{cct} list-column (per-year renewal-pace series); \code{NULL}
(default) omits it.}

\item{entropy}{Optional \code{\link[=sniff_trajectory_entropy]{sniff_trajectory_entropy()}} output (one row per
\code{traj_id}), left-joined to add the \code{keyword_entropy} list-column (per-year
keyword-diversity series); \code{NULL} (default) omits it.}

\item{hubs}{Optional \code{\link[=sniff_trajectory_hubs]{sniff_trajectory_hubs()}} output (one row per
\code{traj_id}), left-joined to add the hub-role columns; \code{NULL} (default) omits
them.}
}
\value{
A tibble, one row per trajectory, sorted by descending
\code{emergence_index} (absorbed last): \code{traj_id}, \code{type}, \code{group}, \code{start}, \code{end},
\code{age}, \code{size}, \code{growth_rate}, \code{doubling_time}, \code{novelty}, \code{recruitment},
\code{emergence_density}, \code{attraction_inflow}, \code{dest_entropy}, \code{dormant_share},
\code{phase}, \code{fate},
\code{state}, \code{emergence_index}, \code{emergence_index_intensive} (the same index
without the extensive \code{recruitment} pillar), \code{reach_ratio}, plus any
\code{cct} / \code{keyword_entropy} (per-year list-columns) and \code{hubs} columns.
}
\description{
Characterizes each \code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}} trajectory along the evolutionary
states of the method (emergence, convergence, divergence, dormancy), grounding
the four emergence pillars (novelty, growth, community, persistence) in two
complementary lenses tied to the flow model.
}
\details{
\strong{Forward lens (all trajectories).} \code{growth_rate} and \code{doubling_time} (an
exponential fit of the recent size curve), \code{novelty} (share of documents that
arrived recently), \code{recruitment} (net documents gained over the lifespan, the
power of attraction), \code{emergence_density} (staying power: the share of the
lineage's year-steps still growing at the emergence rate, after Carley et al.
2017), and \code{age}/\code{size} (persistence and community).

\strong{Backward lens (absorbed trajectories only).} Where the terminal cohort goes,
via \code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}}: \code{dest_entropy} (normalized Shannon
entropy of the destination split) and \code{dormant_share} (share dropping out).

\strong{Inflow lens (central trajectories only).} \code{attraction_inflow}, the documents
of the final community delivered by absorbed tributaries (the consolidating side
of convergence), from \code{\link[=sniff_trajectory_self_sufficiency]{sniff_trajectory_self_sufficiency()}}.

\strong{Reach lens (all trajectories).} \code{reach_ratio}, the share of the future field
a lineage's origin seeds -- its birth node's forward temporal reach in the DAG
over all nodes born later (after the temporal-reachability of Marino & Silva
2023). Structural downstream influence, distinct from size and, via the ratio,
from age.

Classification uses two axes: a life-cycle \strong{\code{phase}} for living (central)
trajectories -- emergence / maturity / dormancy(stall) by growth and novelty --
and a terminal \strong{\code{fate}} for declining (absorbed) ones -- convergence /
divergence / dormancy(death) by destination. \code{state} is \code{phase} for centrals and
\code{fate} for absorbed; \code{phase} and \code{fate} are kept separate so the two senses of
dormancy (a stalled living core vs a dead cohort) stay distinguishable. The
\code{emergence_index} (a within-central combination of growth, novelty and
recruitment) is computed for living trajectories only, so an absorbed lineage
that grew before dying is never ranked as emerging. Because \code{recruitment} is an
extensive (size-carrying) pillar, the full index partly tracks community size;
\code{emergence_index_intensive} drops it (growth and novelty only) for a rate-based
ranking that does not reward a large core for its volume alone.
}
\seealso{
\code{\link[=sniff_trajectory_braid]{sniff_trajectory_braid()}}, \code{\link[=sniff_trajectory_destination]{sniff_trajectory_destination()}},
\code{\link[=sniff_trajectory_self_sufficiency]{sniff_trajectory_self_sufficiency()}}, \code{\link[=sniff_trajectory_cct]{sniff_trajectory_cct()}},
\code{\link[=sniff_trajectory_entropy]{sniff_trajectory_entropy()}}, \code{\link[=sniff_trajectory_hubs]{sniff_trajectory_hubs()}},
\code{\link[=fixed_state_thresholds]{fixed_state_thresholds()}}

Other trajectory analysis: 
\code{\link{sniff_trajectory_cct}()},
\code{\link{sniff_trajectory_community}()},
\code{\link{sniff_trajectory_confluence}()},
\code{\link{sniff_trajectory_contribution}()},
\code{\link{sniff_trajectory_destination}()},
\code{\link{sniff_trajectory_emergence_owners}()},
\code{\link{sniff_trajectory_entropy}()},
\code{\link{sniff_trajectory_formation}()},
\code{\link{sniff_trajectory_hubs}()},
\code{\link{sniff_trajectory_self_sufficiency}()}
}
\concept{trajectory analysis}
